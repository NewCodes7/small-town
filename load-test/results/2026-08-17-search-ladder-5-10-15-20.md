# 검색 사다리 상향 (VU 5/10/15/20) — 2026-08-17

**목적**: 정점을 사다리 안에 담는다. 직전 실행에서 VU10(14.30 RPS)이 최고값이 되어
기존 사다리(1/2/5/10)가 천장을 담지 못했다.
**선행 문서**: [`2026-08-17-osiv-connection-hold-ab.md`](2026-08-17-osiv-connection-hold-ab.md) 7장의
"사다리를 VU 10/15/20으로 확장 — 새 1순위".
**사다리 변경 커밋**: `2060df6` (기본값 1/2/5/10 → 5/10/15/20, 수집 스크립트 3종 파라미터화)

| 구성 | 코드 | 사다리 | testid | 실행 (UTC) |
|---|---|---|---|---|
| Q | `838ae89` | 1/2/5/10 | `20260817-103805` | 08-17 10:38 |
| **R** | `838ae89` (동일 빌드) | **5/10/15/20** | `20260817-111839` | **08-17 11:18** |

- **코드는 Q와 같다** — 재배포 없음(blue 45분 가동 중). 순수하게 사다리만 바꾼 측정이다.
- k6 이미지는 아직 옛 기본값을 갖고 있어(시나리오가 이미지에 굽힘) **`-e VU_LEVELS=5,10,15,20`을
  명시해 실행**했다. 인자 없이 새 기본값을 쓰려면 이미지 재빌드·푸시가 필요하다.
- 실행 전 앱 호스트는 이미 메모리 압박 상태였다: MemAvailable 158MB / 1,906MB,
  SwapFree 445MB / 2,048MB, 무부하에서도 swap-in 370 pages/s.

---

## 헤드라인

**정점은 VU10의 14.3 RPS이고, 직전 실행과 정확히 일치한다. VU15에서 꺾이고 VU20은 측정 불가였다.**

| VU | 완료 2xx | RPS | 5xx | p50(ms) | p95(ms) | DB CPU | 획득대기 | pending | blks/req |
|---|---|---|---|---|---|---|---|---|---|
| 5 | 2,313 | 12.85 | 0 | 356 | 499 | 79% | 0.000s | 0 | 14,107 |
| **10** | **2,579** | **14.33** | 5 | 589 | 959 | 78% | 0.050s | 6 | 13,531 |
| 15 | 1,835 | 10.19 | 1 | 908 | 1,536 | 84% | 0.117s | 16 | **19,915** |
| 20 | 930 | **5.17** | **48** | 1,338 | **18,882** | — | — | — | — |

level_10 정상상태(앞 60초 제외 120초): **14.16 RPS**

### 정점 재현성 — 다른 사다리에서 같은 값

| 실행 | 사다리 | VU10 RPS |
|---|---|---|
| Q | 1/2/5/10 (VU10이 마지막 레벨) | **14.30** |
| R | 5/10/15/20 (VU10이 두 번째 레벨) | **14.33** |

레벨 순서·앞선 레벨의 과도구간이 다른데 값이 0.2% 안에서 일치한다.
**14.3 RPS는 이 빌드의 실제 천장으로 확정한다.**

---

## 1. VU15 — 꺾임의 정체는 버퍼 풀 교체다

VU15에서 처리량이 14.33 → 10.19로 떨어지는데 **DB CPU는 오히려 78% → 84%로 올라간다.**
즉 DB가 더 열심히 일하면서 결과는 덜 나온다. 원인이 `blks/req`에 보인다:

| VU | RPS | blks/req | 요청당 MB |
|---|---|---|---|
| 5 | 12.85 | 14,107 | 110 |
| 10 | 14.33 | 13,531 | 106 |
| **15** | 10.19 | **19,915 (+47%)** | **156** |

**같은 쿼리인데 요청당 블록이 47% 늘었다.** 동시성이 오르면 각 요청의 워킹셋이 서로를 밀어내
같은 블록을 다시 읽는다 — 선행 문서 5.3의 "검색 한 번이 132MB를 256MB 버퍼 풀로 쓸어낸다"가
포화에서 실제로 증폭되는 모습이다. 요청당 비용이 부하에 따라 **커지는** 구조라
동시성을 올리는 것으로는 이 지점을 넘을 수 없다.

동시에 커넥션 압력도 붙는다 — 획득대기 0.050 → 0.117s, pending 6 → 16.

---

## 2. VU20 — 측정 불가 영역 (관측 스택 붕괴)

VU20 구간(11:29:35~11:32:35)에서 **DB·호스트 지표가 전부 비어 있다.** 수집 실패가 아니라
**앱 호스트의 관측 스택이 그 시각에 죽었기 때문**이다.

앱 호스트 메모리 타임라인 (2분 간격, 지표가 끊기는 지점이 사망 시각):

| 시각 | 레벨 | MemAvailable | swap-in |
|---|---|---|---|
| 11:18 | (시작 전) | 167MB | 189 pages/s |
| 11:20 | VU5 | 120MB | 978 |
| 11:22 | VU10 | 111MB | 2,541 |
| 11:24 | VU10 | **92MB** | 2,557 |
| 11:26 | VU15 | 124MB | 2,511 |
| 11:28 | VU15 | 172MB | 1,837 |
| 11:30 | VU20 | **(지표 끊김)** | 1,111 |

호스트 RAM 1,906MB에서 가용 92MB까지 내려갔고, VU20 진입과 함께
**Prometheus·Grafana·Loki·Tempo가 응답 불가**가 됐다. 부하 종료 후 20분이 지나도 회복되지 않아
(`/api/categories` 3~14초, Prometheus 000) **호스트 재부팅으로 복구**했다.
재부팅 후 Prometheus가 WAL을 재생하며 503을 거쳐 200으로 돌아왔고, **다행히 실행 데이터는 보존됐다** —
이 문서의 수치는 재부팅 이후 재수집한 것이다.

VU20의 5xx 48건은 정직한 풀 포화다:
`HikariPool-1 ... timed out after 3165ms (total=5, active=5, idle=0, waiting=16)` —
타임아웃이 설정값(3,000ms)대로 찍혔다. VU20은 요청당 커넥션 2개가 필요한 구조에서
동시 20 요청 = 수요 40 대 공급 5이므로 붕괴가 당연하다.

> **직전 실행(Q)의 8.4초 타임아웃과 다르다.** Q에서는 4.15초 GC 정지 때문에 스레드가 늦게 깨어나
> 3초 설정이 8.4초로 관측됐다(선행 문서 「다음」 항목). VU20에서는 설정값대로 3.1초라,
> 이 구간의 지배 요인은 GC가 아니라 **순수한 풀 초과 수요**다.

---

## 3. 유효성 검증 — 키워드 소진은 없었다

2-term 조합은 레벨당 2,525개인데 VU10이 2,579건을 처리해 슬롯을 넘겼다. 초과분이 다음 레벨
구간을 당겨쓰면 5분 TTL 캐시 히트가 생겨 VU15가 과대평가된다. README의 판별법으로 확인했다:

| 구간 | 완료 2xx | Loki `총:` (코어 실행 수) | 판정 |
|---|---|---|---|
| VU15 (11:26:05~11:29:05) | 1,835 | **2,604** | 캐시 히트 없음 ✅ |

캐시 히트가 있었다면 `총:`이 완료 건수보다 **적게** 나온다. 반대로 많은 것은 창 경계를 넘어
완료된 요청 때문이며(p50 908ms), 코어가 매 요청 실제로 돌았다는 뜻이다.

---

## 4. 판정

| 주장 | 판정 |
|---|---|
| 사다리 상향으로 정점을 담는다 | ✅ **성공.** 정점(VU10)과 꺾임(VU15)이 모두 범위 안에 들어왔다 |
| 천장 = 14.3 RPS | ✅ **확정.** 서로 다른 사다리의 두 실행이 14.30 / 14.33 |
| 천장의 제약은 무엇인가 | **DB가 주(정점에서 CPU 78%)이고 커넥션이 종(획득대기 0.050s 발생).** VU15에서는 요청당 블록이 47% 늘어 DB 쪽이 먼저 무너진다 |
| VU20은 유효한 측정 구간인가 | ❌ **아니다.** 앱 호스트가 관측 스택과 함께 붕괴해 지표가 남지 않는다 |

### 사다리 권고 (다음 실험부터)

**5/10/15까지만 쓴다.** VU20은 (a) 데이터가 안 남고 (b) 프로덕션을 20분 이상 degrade시킨다.
호스트 메모리를 정리한 뒤에야 재시도 대상이다.

---

## 5. 앱 호스트 메모리 — 누가 차지했나

2장의 관측 스택 붕괴와 「다음」의 1순위를 정산하려고, 실행 시각의 Prometheus 데이터를 **사후 조회**해
앱 호스트 메모리를 분해했다(재부팅 전 데이터가 보존된 덕에 가능했다).
**결론: 1순위는 관측 스택이 아니라 백엔드 JVM의 커밋 힙이고, 2순위인 Prometheus는
k6 remote-write의 URL 카디널리티가 원인이다.**

### 5.1 총수요는 RAM의 1.6배였다

11:24 UTC(VU10 말~VU15 초, 압박 최대) 실측:

| 항목 | 값 |
|---|---|
| MemTotal | 1,906 MiB |
| AnonPages (프로세스 상주) | **1,474 MiB** |
| Swap used (2,048 중) | **1,530 MiB** |
| Cached / Slab / PageTables | 117 / 98 / 18 MiB |
| MemAvailable | 92 MiB |

**익명 메모리 총수요 = 1,474 + 1,530 ≈ 3,004 MiB = RAM의 1.6배.** 페이지캐시는 117MB까지
쪼그라들어 있었다 — 1장의 DB 쪽 `blks/req` 증가와는 **별개의 축**으로, 앱 호스트에는 캐시가
남아 있지 않았다.

### 5.2 1순위 — 백엔드 JVM: used 337MB가 아니라 **committed 984MB**

「다음」에 적었던 "힙 276MB/1,024MB로 여유가 있다"는 **used 기준의 오독**이다.
호스트가 물리 메모리 비용을 치르는 건 committed다. 11:24 실측:

| 영역 | committed | used |
|---|---|---|
| G1 Old Gen | 841 MiB | 258 MiB |
| G1 Eden | 81 MiB | 63 MiB |
| G1 Survivor | 16 MiB | 15 MiB |
| **heap 합** | **984 MiB** | 337 MiB |
| Metaspace | 158 MiB | 155 MiB |
| CodeHeap + Compressed Class | 87 MiB | 68 MiB |
| **nonheap 합** | **244 MiB** | 206 MiB |
| direct buffer | 27 MiB | — |

- JVM 커밋 총합 ≈ **1,255 MiB**, 스레드 스택·GC·JIT 네이티브까지 더하면 **약 1.3 GB = 호스트 RAM의 68%**
- `-Xms512m -Xmx1024m`(`Dockerfile:16`)에서 G1이 부하 중 상한까지 확장했고, 확장분은 쓰지 않아도
  즉시 반납되지 않아 그대로 스왑 대상이 된다
- 대조군: **같은 blue의 idle 커밋 힙은 542 MiB**(08-18 측정). 부하 때만 +440MB가 붙는 구조다

### 5.3 2순위 — Prometheus: k6가 검색 키워드를 라벨에 실어 보냈다

11:24 기준 **활성 시계열 158,716개**의 출처:

| 출처 | 시계열 수 |
|---|---|
| job 라벨 없음 = **k6 remote-write** | **151,911 (96%)** |
| postgres_exporter | 3,324 |
| backend | 2,470 |
| node-exporter ×2 (앱·DB) | 1,011 |

원인은 k6 태그다. 실제 시계열 라벨을 뽑으면 `name`/`url`이 **쿼리스트링까지 포함한 전체 URL**이다:

```
name = "https://newcodes.net/api/search/articles/loadtest?keyword=opentelemetry%20코루틴&page=1&size=10"
url  = (같은 값)
```

시나리오가 `tags: { endpoint: 'search', level }`만 주고 `name`을 덮어쓰지 않으며
(`scenarios/ramp-limit-finder.js:97`), `systemTags`로 `url`을 끄지도 않아 k6 기본값이 그대로 라벨이 된다.
키워드가 요청마다 다르니 **키워드 수 = 카디널리티**가 된다:

- `k6_http_req_duration_p95` 한 메트릭에 **3,452 시계열** (= 유니크 URL 수)
- k6 메트릭 이름 **56개** (`K6_PROMETHEUS_RW_TREND_STATS`의 통계 6종 × trend 8종 + 카운터/게이지)
- → **실행 1회당 15만~19만 시계열**. 게다가 `testid`가 매번 달라 실행끼리 절대 합쳐지지 않는다
- 최근 1시간(Q 10:38 + R 11:18) 창으로 세면 그 한 메트릭만 **11,755 시계열** →
  head 전체로는 **약 65만 시계열** 규모다 (stale 시계열도 2시간 블록이 잘릴 때까지 head에 남는다)

Prometheus 2.37의 head 시계열당 통상 3~4 KiB로 잡으면 **head+WAL만 대략 1~2 GB**다.
VU20에서 관측 스택이 통째로 죽고 재부팅 후 Prometheus가 큰 WAL을 재생하며 503을 거친 것과 맞는다.
같은 시각 **백엔드는 설정값대로 3.1초 Hikari 타임아웃을 정직하게 냈다**(2장) — 먼저 무너진 쪽은
백엔드가 아니라 Prometheus다.

누적 증거도 있다. 익명 총수요(anon + swap used)는 10:00 2,262 MiB → 11:20 2,525 MiB로 **+263 MiB**
늘었는데, 이 구간의 배포(green→blue 교체, 10:32)로 설명되는 몫은 +120 MiB뿐이다. 나머지는 Q 실행이
남긴 시계열이 head에 계속 머문 몫으로 본다.

### 5.4 메모리 장부 (11:24)

| 프로세스 | MiB | 근거 |
|---|---|---|
| **backend-blue JVM** | **~1,300** | **실측** (committed heap+nonheap+direct) |
| **Prometheus** | ~700–1,200 | **추정** — 시계열 수는 실측, 바이트/시계열은 통상값 |
| Tempo + Loki + promtail + Grafana | ~300–450 | 추정. `management.tracing.sampling.probability=1.0`이라 전 요청이 트레이싱된다 |
| nginx, exporter 3종, dockerd, 그 외 | ~150–200 | exporter RSS만 실측 (각 12~15 MB) |
| **합** | **~2.4–3.1 GB** | 실측 총수요 3.0 GB와 정합 |

### 5.5 이 분해의 한계와 확정 방법

**앱 호스트에는 cAdvisor도 process-exporter도 없어 컨테이너별 RSS가 Prometheus에 남지 않는다.**
위 표에서 JVM과 exporter만 실측이고, Prometheus·Tempo·Loki·Grafana는 추정이다. 확정하려면
운영 셸에서 아래를 남겨야 한다:

```bash
# 현재 상태(idle 기준선)
docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}"
smem -k -s swap -r | head -20        # 스왑까지 포함한 프로세스별 점유 (apt install smem)

# 다음 사다리 실행 때 같이 띄워둘 것 — 추정 칸이 실측으로 바뀐다
while true; do docker stats --no-stream --format '{{.Name}} {{.MemUsage}}' \
  | sed "s/^/$(date -u +%H:%M:%S) /" >> /tmp/memstat.log; sleep 10; done &
```

---

### 5.6 조치와 예상 절감

절감 추정의 전제: Prometheus 몫을 총수요에서 역산하면
`3,004 − JVM 1,300 − (Tempo·Loki·Grafana·nginx·시스템 450~650) ≈ **1,050~1,250 MiB**`이고,
이를 head 시계열 35만~65만으로 나누면 **시계열당 2~3 KiB** — Prometheus 2.x의 통상값과 맞는다.
아래 절감액은 이 역산에 기댄 추정이며, 5.5의 `docker stats` 측정으로 확정해야 한다.

| # | 조치 | 예상 절감 | 방법론 영향 |
|---|---|---|---|
| **1** | k6 `name`/`url` 라벨 제거 (`--system-tags`) | **700~1,100 MiB** (실행당 시계열 19만 → 500 미만, **−99.7%**) | ⚠️ p50/p95 산출식 교체 필요 (아래) |
| **1′** | `K6_PROMETHEUS_RW_TREND_STATS`를 `p(50),p(95),p(99)`로 축소 | ~350~500 MiB (메트릭 이름 56 → 32, −43%) | **없음** — `avg/min/max`는 소비자가 없다. `p(99)`는 대시보드 패널이 쓰므로 남긴다 |
| 2 | `-Xmx1024m` → `512m` | ~440 MiB (부하 중 커밋 힙 984 → ~540) | 없음. used 최대 337MB라 여유 있다 |
| 3 | 컨테이너 `mem_limit` 부여 | (절감 아님) 호스트 전체 스래싱 대신 컨테이너 1개 OOM-kill로 국한하는 안전장치 | 없음 |
| 4 | `TRACING_SAMPLING=1.0 → 0.1` | ~~수십~150 MiB~~ → **5.7 실측 결과 Tempo가 544 MiB로 2위. 우선순위 2위로 상향** | 트레이스 표본 1/10 |

**1번만 반영했을 때**: 총수요 3,004 → **약 1,950~2,300 MiB**(RAM 1,906 대비 1.6배 → **1.0~1.2배**).
스왑 사용은 1,530MB → 200~400MB, swap-in은 2,500 pages/s → 수백 대로 떨어져
**관측 스택이 VU20을 넘길 가능성이 생긴다.** 다만 여유는 여전히 없다 —
**1번 + 2번을 같이 해야 총수요가 1,500~1,900 MiB로 RAM 안에 들어온다.**

**절감되지 않는 것**: 14.3 RPS 천장은 DB 제약이라 그대로다(1·4장). VU20의 5xx 48건도
수요 40 대 공급 5의 풀 문제라 그대로다. ingest 표본율도 원래 낮다(14 RPS × 56 메트릭 ≈ 800 samples/s) —
**이 조치의 절감은 CPU가 아니라 head 메모리와 인덱스다.** 부수 효과로 WAL이 작아져
재부팅 후 replay 시간(2장의 503 구간)도 함께 줄어든다.

> **⚠️ 1번의 숨은 의존성.** `collect-results.py:87`의 p50/p95는 **URL별 시계열을 요청 1건으로 간주해
> `quantile()`로 재구성**한다(코드 주석에 명시). `name`/`url`을 없애면 레벨당 시계열이 소수로 붕괴해
> 이 식이 무의미해진다 — 에러 없이 p50과 p95가 같은 값이 나오는, 가장 위험한 형태로 틀린다.
> **→ 2026-08-18 해결됨. 산출식 교체와 실측 캘리브레이션은 5.7 참고.**

### 5.7 조치 1+1′ 시행 (2026-08-18)

**적용 지점은 시나리오가 아니라 실행 레이어다** — `run-task.sh`/`run-local.sh`가 `--system-tags`와
`K6_PROMETHEUS_RW_TREND_STATS`를 넘긴다. 시나리오 11개를 안 건드리므로 **ECR 이미지 재빌드가 없고**,
`LT_SYSTEM_TAGS=`(빈 값) 한 번으로 즉시 롤백된다.

| 항목 | 값 |
|---|---|
| 유지 태그 | `status,method,error_code,check,group,scenario,expected_response,proto` |
| 제외 태그 | `url`, `name`, `error`, `tls_version`, `subproto`, `service` |
| TREND_STATS | `p(50),p(95),p(99)` (`avg,min,max` 제거) |
| 로컬 검증 | 방출 라벨에서 `url`/`name`/`error`/`tls_version` 소멸, `expected_response`·`endpoint`·`testid` 생존 확인 |

**산출식 교체 — 시간축 집계 함수를 실측으로 정했다.** k6 PRW의 trend sink는 flush 창별이 아니라
**시계열별로 누적**이다. R의 `k6_iteration_duration_p95{scenario="level_10"}`가
0.764 → 1.017 → 0.900 → … → **0.9497에서 수렴 후 고정**되는 것으로 확인했다(창 단위였다면 진동한다).
따라서 레벨 종료 시점 값이 곧 그 레벨 전체의 분위수이고, **`last_over_time`이 정답**이다 —
`avg_over_time`은 수렴 전 구간을 섞어 과소(−3%), `max_over_time`은 과도구간 피크를 집어 과대(+7%)다.

**캘리브레이션 — 신·구 차이는 ≤1.8%다.** 같은 실행(R) 안에서 두 방식을 모두 산출했다
(과거 실행은 `url` 라벨이 없는 `iteration_duration`으로 폴백 — ramp-limit-finder는 iteration당 요청 1회):

| VU | legacy p50/p95 | native p50/p95 | Δ |
|---|---|---|---|
| 5 | 356 / 499 | 354 / 490 | −0.6% / **−1.8%** |
| 10 | 589 / 959 | 595 / 950 | +1.0% / −0.9% |
| 15 | 908 / 1,536 | 908 / 1,536 | 0% / 0% |

**시계열 불연속은 사실상 없다.** 또한 legacy 모드가 이 문서 헤드라인 표의 네 레벨을 모두 정확히
재현하는 것으로 회귀 검증했다(VU20의 1,338 / 18,882 포함).

**부수 정정 2건**:
- 자동판별을 시계열 *개수*로 하면 VU20처럼 관측 스택이 죽어 데이터가 희박한 구간에서 오판한다 —
  **`url` 라벨의 유무**로 가르도록 고쳤다.
- 대시보드의 `avg(k6_http_req_duration_p*)` 패널 5개는 지금까지 URL별 수천 시계열의 평균이라
  자연스럽게 요청 수 가중이었는데, 라벨 축소 후에는 5건짜리 5xx가 2,500건짜리 2xx와 1:1로 평균된다 —
  `expected_response="true"` 필터를 쿼리에 추가했다.

#### 전환 실행 S (`20260818-075257`, VU 5/10/15)

**카디널리티 — 목표 초과 달성.**

| | R (08-17) | S (08-18) |
|---|---|---|
| 실행당 k6 시계열 | ~193,000 | **86** |
| Prometheus 전체 활성 시계열 | 158,716 | **9,512** |

수집 스크립트는 `native` 모드를 자동 판별했고 p50<p95 정상이다.

**5.4 장부 확정 — 추정을 실측이 갈아엎었다.** 앱 호스트 `docker stats`(08:04:06 UTC, 피크):

| 컨테이너 | 실측 MiB | 5.4의 추정 |
|---|---|---|
| newcodes-backend-blue | **684** (RSS) | ~1,300 (커밋 기준) |
| **tempo** | **544** | Loki·Grafana와 뭉뚱그려 300~450 |
| prometheus | **73** | 700~1,200 ❌ |
| grafana / loki | 48 / 42 | |
| exporter 3종 + nginx | 44 | 150~200 |
| **합** | **1,436** | |

- **Prometheus는 73 MiB로 무해해졌다** — 조치 1이 정확히 표적을 맞혔다(08:04:54의 204 MiB는 WAL/컴팩션 순간값).
- **5.4의 Prometheus 추정은 2배 이상 과대했다.** 158k 시계열이면 300~500 MiB가 현실적이고, 총수요에서
  역산할 때 차액을 전부 Prometheus에 몰아준 것이 오류였다. **그 차액의 실체는 Tempo다** —
  `management.tracing.sampling.probability=1.0`으로 전 요청이 트레이싱되고 `mem_limit`도 없다.
  조치 우선순위에서 **트레이싱 표본 축소가 4위 → 2위로 올라간다.**
- backend RSS 684는 커밋(heap 840 + nonheap 271 = 1,111)보다 작다 — `docker stats`는 상주분만 보여주고
  커밋·미접촉·스왑아웃 페이지는 안 잡는다. 호스트를 무너뜨리는 건 커밋 쪽이라 조치 2는 여전히 유효하다.

**호스트 — 관측 스택이 살아남았다.**

| | R 피크 | S 피크 |
|---|---|---|
| 총수요(anon+swap) | 3,004 MiB | 2,879 MiB |
| MemAvailable 최저 | **92 MiB** | **164 MiB** |
| GC 최대 정지 | 4.15s | 2.52s |
| 관측 스택 | VU20에서 사망 → 재부팅 | **정상** |

총수요가 125 MiB밖에 안 줄어든 것은 위 장부로 설명된다 — Prometheus에서 회수한 몫을 Tempo가
그대로 차지하고 있었고, 부하 종료 시점에 JVM 힙이 512 → 840 MiB로 확장했다.

**처리량 — 「천장 14.3 RPS 확정」이 흔들린다.**

| VU | R: 2xx / RPS / p95 / blks_req | S: 2xx / RPS / p95 / blks_req |
|---|---|---|
| 5 | 2,313 / 12.85 / 499 / 14,107 | 2,389 / 13.27 / 479 / 14,053 |
| 10 | 2,579 / 14.33 / 959 / 13,531 | 2,820 / **15.67** / 825 / 13,601 |
| 15 | 1,835 / 10.19 / 1,536 / **19,915** | 2,638 / **14.66** / 1,243 / **14,077** |

**VU15의 꺾임이 사라졌고 1장이 진단한 `blks/req +47%`가 재현되지 않았다**(14,077로 평평).
VU10은 15.67로 헤드라인의 천장 14.3을 넘었다. 다만 **이 개선을 조치 1의 공으로만 돌릴 수 없다** —
R 이후 호스트 재부팅이 있었고 S 실행 중 JVM 커밋 힙은 512 MiB였다(R은 984). 공통 원인은
**앱 호스트 메모리 압박 해소**이고, k6 몫과 재부팅 몫은 이 실행 하나로 분리되지 않는다.
유력해진 해석은 **1장의 `blks/req` 증가가 DB 자체 성질이 아니라, 앱이 느려져 DB 동시 요청이 쌓인
2차 효과**였다는 것이다. 헤드라인의 「14.3 = 이 빌드의 실제 천장」은 **메모리 압박 상태에서의 천장**으로
한정해 읽어야 한다 — 재측정이 필요하다.

---

## 6. 한계

- **VU20 구간의 원인 분해가 불가능하다** — 그 시각 DB/호스트 지표가 없다. 풀 포화는 앱 로그로
  확인했지만, 호스트 메모리가 얼마나 기여했는지는 측정되지 않았다.
- R은 1회 실행이다. 다만 정점 값은 Q와 교차 확인됐다.
- 실행 전부터 스왑 압박(SwapFree 445MB)이 있었으므로 **VU5의 12.85 RPS는 Q의 VU5(10.92)와
  직접 비교하기 어렵다.** 레벨 순서도 다르다(첫 레벨 vs 세 번째 레벨).
- **5장의 컨테이너별 배분은 추정이다** — cAdvisor/process-exporter가 없어 JVM과 exporter만 실측이고,
  Prometheus·Tempo·Loki·Grafana 몫은 시계열 수와 총수요에서 역산했다 (확정 방법은 5.5).
- 앱 호스트 메모리 문제는 원인만 밝혔을 뿐 조치는 아직이다 — 5.6과 아래 참고.

---

## 미기록 / 다음

- [x] 사다리 상향 (선행 문서 7장 1순위) — 이 문서. 정점 VU10 = 14.3 RPS 확정
- [x] **앱 호스트 메모리 분해** — 5장. 1순위는 백엔드 JVM의 **커밋 힙 984MB**(used는 337MB뿐 —
      종전 "힙 276MB/1,024MB로 여유"는 used 기준 오독이었다), 2순위는 k6 URL 카디널리티로 부푼
      Prometheus(실행당 시계열 15만~19만). 관측 스택이 백엔드와 동거하며 RAM 1,906MB를 나눠 쓴 결과
      총수요가 RAM의 1.6배(3.0GB)까지 갔다
- [x] **k6 라벨 카디널리티 제거(1) + TREND_STATS 축소(1′)** — 5.7. 산출식 교체·캘리브레이션까지 완료.
      1′을 먼저 하고 1을 나중에 하려던 순서는 **동시 시행으로 바꿨다** — 둘 다 실행 레이어 한 줄이라
      변경을 쪼갤 이유가 없고, 쪼개면 "새 실행 + 옛 산출식"이라는 조용히 틀리는 조합이 생긴다
- [x] **전환 실행(S, `20260818-075257`, VU 5/10/15)** — 시계열 86개, Prometheus 73 MiB,
      관측 스택 생존. `docker stats` 실측으로 5.4 장부 확정 (5.7)
- [ ] **Tempo 억제 — 새 2순위(실측 544 MiB).** `TRACING_SAMPLING=0.1` + `mem_limit` + `block_retention`
      168h 재검토. 5.4에서 이 몫을 Prometheus로 오귀속했던 것이 이번에 바로잡혔다
- [ ] **`-Xmx1024m` → `512m`** — S 실행 중 커밋 힙은 512였다가 종료 시점에 840으로 확장했다.
      RSS(684)와 커밋(1,111)의 간극이 스왑 압력의 정체다
- [ ] **천장 재측정** — S에서 VU10 15.67 / VU15 14.66으로 「천장 14.3」과 VU15 꺾임이 모두 재현되지
      않았다. 메모리 압박이 없는 상태에서 사다리를 다시 돌려 진짜 천장과 꺾임점을 잡을 것
      (재부팅 몫과 조치 1 몫을 가르려면 같은 호스트 상태에서 `LT_SYSTEM_TAGS=` A/B가 필요하다)
- [ ] **k6 이미지 재빌드·푸시** — 시나리오 기본값(5/10/15/20)이 ECR 이미지에 반영돼야
      `-e VU_LEVELS` 없이도 새 사다리로 돈다
- [ ] 과거 실행 재수집 시 `VU_LEVELS=1,2,5,10`을 지정해야 한다 (스크립트 기본값이 바뀜)
- [ ] Q/R 반복 실행 — 정점은 교차 확인됐지만 VU15 꺾임 폭은 1회뿐
- [ ] C안: 소프트 삭제 시 청크 정리 (선행 문서 7장에서 이월)
- [ ] `ef_search`/`candidateLimit`(250/200) 하향 스윕 — VU15의 blks/req +47%는 이 레버의 근거를 강화한다
- [ ] recall 측정 (`VectorSearchAccuracyService`) — 기존 부채
