# 검색 사다리 상향 (VU 5/10/15/20) — 2026-08-17

**목적**: 정점을 사다리 안에 담는다. 직전 실행에서 VU10(14.30 RPS)이 최고값이 되어
기존 사다리(1/2/5/10)가 천장을 담지 못했다.
**선행 문서**: [`2026-08-17-osiv-connection-hold-ab.md`](2026-08-17-osiv-connection-hold-ab.md) 7장의
"사다리를 VU 10/15/20으로 확장 — 새 1순위".
**사다리 변경 커밋**: `2060df6` (기본값 1/2/5/10 → 5/10/15/20, 수집 스크립트 3종 파라미터화)

| 구성 | 코드 | 사다리 | testid | 실행 (UTC) |
|---|---|---|---|---|
| Q | `838ae89` | 1/2/5/10 | `20260817-103805` | 08-17 10:38 |
| **R** | `838ae89` (동일 빌드) | **5/10/15/20** | `20260817-111839` | **08-17 11:18** |

- **코드는 Q와 같다** — 재배포 없음(blue 45분 가동 중). 순수하게 사다리만 바꾼 측정이다.
- k6 이미지는 아직 옛 기본값을 갖고 있어(시나리오가 이미지에 굽힘) **`-e VU_LEVELS=5,10,15,20`을
  명시해 실행**했다. 인자 없이 새 기본값을 쓰려면 이미지 재빌드·푸시가 필요하다.
- 실행 전 앱 호스트는 이미 메모리 압박 상태였다: MemAvailable 158MB / 1,906MB,
  SwapFree 445MB / 2,048MB, 무부하에서도 swap-in 370 pages/s.

---

## 헤드라인

**정점은 VU10의 14.3 RPS이고, 직전 실행과 정확히 일치한다. VU15에서 꺾이고 VU20은 측정 불가였다.**

| VU | 완료 2xx | RPS | 5xx | p50(ms) | p95(ms) | DB CPU | 획득대기 | pending | blks/req |
|---|---|---|---|---|---|---|---|---|---|
| 5 | 2,313 | 12.85 | 0 | 356 | 499 | 79% | 0.000s | 0 | 14,107 |
| **10** | **2,579** | **14.33** | 5 | 589 | 959 | 78% | 0.050s | 6 | 13,531 |
| 15 | 1,835 | 10.19 | 1 | 908 | 1,536 | 84% | 0.117s | 16 | **19,915** |
| 20 | 930 | **5.17** | **48** | 1,338 | **18,882** | — | — | — | — |

level_10 정상상태(앞 60초 제외 120초): **14.16 RPS**

### 정점 재현성 — 다른 사다리에서 같은 값

| 실행 | 사다리 | VU10 RPS |
|---|---|---|
| Q | 1/2/5/10 (VU10이 마지막 레벨) | **14.30** |
| R | 5/10/15/20 (VU10이 두 번째 레벨) | **14.33** |

레벨 순서·앞선 레벨의 과도구간이 다른데 값이 0.2% 안에서 일치한다.
~~**14.3 RPS는 이 빌드의 실제 천장으로 확정한다.**~~

> **⚠️⚠️ 폐기가 다시 철회됐다 (9장, 2026-08-20).** 아래 폐기 근거였던 런 T~X는 **벡터 검색이
> 실행되지 않은 상태**였다(mock 미기동). 벡터를 켜고 다시 돌린 런 Y가 `blks/req` 13,246~13,930으로
> **R·S를 그대로 재현**했다. **14.3 RPS는 하이브리드 검색의 천장으로 유효하다.**
> 아래 문단은 기록으로 남긴다.
>
> **⚠️ (철회된) 폐기 근거 (7·8장, 2026-08-19).** 같은 빌드로 돌린 런 T가 **VU10 24.59 RPS / 5xx 0**을
> 냈고 VU15 꺾임도 사라졌다. 지배 변수는 DB의 요청당 논리 읽기가 13,601 → 2,120으로 **6.4배**
> 줄어든 것인데, **그 원인은 아직 특정되지 않았다**(7.4).
> 사다리를 1/2/3/5로 내린 **런 U에서 무릎이 VU2 ≈ 22 RPS**로 잡혔다 — SLA(무부하 대비 120%)를
> 만족하는 운용 용량이고 포화 상한은 23.6 RPS다(8장). 14.3은 "당시 DB 조건에서의 천장"으로만 읽을 것.

---

## 1. VU15 — 꺾임의 정체는 버퍼 풀 교체다

VU15에서 처리량이 14.33 → 10.19로 떨어지는데 **DB CPU는 오히려 78% → 84%로 올라간다.**
즉 DB가 더 열심히 일하면서 결과는 덜 나온다. 원인이 `blks/req`에 보인다:

| VU | RPS | blks/req | 요청당 MB |
|---|---|---|---|
| 5 | 12.85 | 14,107 | 110 |
| 10 | 14.33 | 13,531 | 106 |
| **15** | 10.19 | **19,915 (+47%)** | **156** |

> **⚠️ 이 장의 진단은 조건부다 (7장).** S에서 이미 재현되지 않았고(14,077로 평평), T에서는 요청당
> 블록 자체가 2,153으로 6.4배 낮아지며 꺾임이 사라졌다. 아래 "요청당 비용이 부하에 따라 커진다"는
> **당시 DB 조건(요청당 106 MB > shared_buffers 256 MB)에서만 성립**한다 — 요청당 16.5 MB가 되면
> 애초에 버퍼 풀을 쓸어내지 않으므로 증폭 자체가 일어나지 않는다.

**같은 쿼리인데 요청당 블록이 47% 늘었다.** 동시성이 오르면 각 요청의 워킹셋이 서로를 밀어내
같은 블록을 다시 읽는다 — 선행 문서 5.3의 "검색 한 번이 132MB를 256MB 버퍼 풀로 쓸어낸다"가
포화에서 실제로 증폭되는 모습이다. 요청당 비용이 부하에 따라 **커지는** 구조라
동시성을 올리는 것으로는 이 지점을 넘을 수 없다.

동시에 커넥션 압력도 붙는다 — 획득대기 0.050 → 0.117s, pending 6 → 16.

---

## 2. VU20 — 측정 불가 영역 (관측 스택 붕괴)

VU20 구간(11:29:35~11:32:35)에서 **DB·호스트 지표가 전부 비어 있다.** 수집 실패가 아니라
**앱 호스트의 관측 스택이 그 시각에 죽었기 때문**이다.

앱 호스트 메모리 타임라인 (2분 간격, 지표가 끊기는 지점이 사망 시각):

| 시각 | 레벨 | MemAvailable | swap-in |
|---|---|---|---|
| 11:18 | (시작 전) | 167MB | 189 pages/s |
| 11:20 | VU5 | 120MB | 978 |
| 11:22 | VU10 | 111MB | 2,541 |
| 11:24 | VU10 | **92MB** | 2,557 |
| 11:26 | VU15 | 124MB | 2,511 |
| 11:28 | VU15 | 172MB | 1,837 |
| 11:30 | VU20 | **(지표 끊김)** | 1,111 |

호스트 RAM 1,906MB에서 가용 92MB까지 내려갔고, VU20 진입과 함께
**Prometheus·Grafana·Loki·Tempo가 응답 불가**가 됐다. 부하 종료 후 20분이 지나도 회복되지 않아
(`/api/categories` 3~14초, Prometheus 000) **호스트 재부팅으로 복구**했다.
재부팅 후 Prometheus가 WAL을 재생하며 503을 거쳐 200으로 돌아왔고, **다행히 실행 데이터는 보존됐다** —
이 문서의 수치는 재부팅 이후 재수집한 것이다.

VU20의 5xx 48건은 정직한 풀 포화다:
`HikariPool-1 ... timed out after 3165ms (total=5, active=5, idle=0, waiting=16)` —
타임아웃이 설정값(3,000ms)대로 찍혔다. VU20은 요청당 커넥션 2개가 필요한 구조에서
동시 20 요청 = 수요 40 대 공급 5이므로 붕괴가 당연하다.

> **직전 실행(Q)의 8.4초 타임아웃과 다르다.** Q에서는 4.15초 GC 정지 때문에 스레드가 늦게 깨어나
> 3초 설정이 8.4초로 관측됐다(선행 문서 「다음」 항목). VU20에서는 설정값대로 3.1초라,
> 이 구간의 지배 요인은 GC가 아니라 **순수한 풀 초과 수요**다.

---

## 3. 유효성 검증 — 키워드 소진은 없었다

2-term 조합은 레벨당 2,525개인데 VU10이 2,579건을 처리해 슬롯을 넘겼다. 초과분이 다음 레벨
구간을 당겨쓰면 5분 TTL 캐시 히트가 생겨 VU15가 과대평가된다. README의 판별법으로 확인했다:

| 구간 | 완료 2xx | Loki `총:` (코어 실행 수) | 판정 |
|---|---|---|---|
| VU15 (11:26:05~11:29:05) | 1,835 | **2,604** | 캐시 히트 없음 ✅ |

캐시 히트가 있었다면 `총:`이 완료 건수보다 **적게** 나온다. 반대로 많은 것은 창 경계를 넘어
완료된 요청 때문이며(p50 908ms), 코어가 매 요청 실제로 돌았다는 뜻이다.

---

## 4. 판정

| 주장 | 판정 |
|---|---|
| 사다리 상향으로 정점을 담는다 | ✅ **성공.** 정점(VU10)과 꺾임(VU15)이 모두 범위 안에 들어왔다 |
| 천장 = 14.3 RPS | ~~✅ 확정~~ → ❌ **폐기 (7장).** 같은 빌드의 런 T가 24.59 RPS. 당시 조건 한정값이었다 |
| 천장의 제약은 무엇인가 | **DB가 주이고 커넥션이 종.** 단 7장에서 제약의 성격이 바뀌었다 — 당시는 **I/O 대기**(iowait 0.52 cores)가 지배했고, T에서는 iowait가 0이 되며 **순수 CPU 포화**(1.96/2 cores)로 이동했다 |
| VU20은 유효한 측정 구간인가 | ❌ **아니다.** 앱 호스트가 관측 스택과 함께 붕괴해 지표가 남지 않는다 |

### 사다리 권고 (다음 실험부터)

~~**5/10/15까지만 쓴다.**~~ VU20은 (a) 데이터가 안 남고 (b) 프로덕션을 20분 이상 degrade시킨다.

> **⚠️ 2026-08-19 갱신 (7.1.1).** DB 비용이 6.4배 싸지면서 **정점이 VU5 아래로 내려갔다** —
> VU5/10/15의 RPS가 23.1/24.6/23.9로 평평하고 p95만 2.9배가 된다. 이제 **1/2/3/5**를 쓴다.
> 사다리는 "천장을 담는" 도구라 병목이 바뀔 때마다 재조정 대상이다.

---

## 5. 앱 호스트 메모리 — 누가 차지했나

2장의 관측 스택 붕괴와 「다음」의 1순위를 정산하려고, 실행 시각의 Prometheus 데이터를 **사후 조회**해
앱 호스트 메모리를 분해했다(재부팅 전 데이터가 보존된 덕에 가능했다).
**결론: 1순위는 관측 스택이 아니라 백엔드 JVM의 커밋 힙이고, 2순위인 Prometheus는
k6 remote-write의 URL 카디널리티가 원인이다.**

### 5.1 총수요는 RAM의 1.6배였다

11:24 UTC(VU10 말~VU15 초, 압박 최대) 실측:

| 항목 | 값 |
|---|---|
| MemTotal | 1,906 MiB |
| AnonPages (프로세스 상주) | **1,474 MiB** |
| Swap used (2,048 중) | **1,530 MiB** |
| Cached / Slab / PageTables | 117 / 98 / 18 MiB |
| MemAvailable | 92 MiB |

**익명 메모리 총수요 = 1,474 + 1,530 ≈ 3,004 MiB = RAM의 1.6배.** 페이지캐시는 117MB까지
쪼그라들어 있었다 — 1장의 DB 쪽 `blks/req` 증가와는 **별개의 축**으로, 앱 호스트에는 캐시가
남아 있지 않았다.

### 5.2 1순위 — 백엔드 JVM: used가 아니라 **committed가 비용이다** (used 수치는 5.9에서 정정)

「다음」에 적었던 "힙 276MB/1,024MB로 여유가 있다"는 **used 기준의 오독**이다.
호스트가 물리 메모리 비용을 치르는 건 committed다. 11:24 실측:

| 영역 | committed | used |
|---|---|---|
| G1 Old Gen | 841 MiB | 258 MiB |
| G1 Eden | 81 MiB | 63 MiB |
| G1 Survivor | 16 MiB | 15 MiB |
| **heap 합** | **984 MiB** | 337 MiB |
| Metaspace | 158 MiB | 155 MiB |
| CodeHeap + Compressed Class | 87 MiB | 68 MiB |
| **nonheap 합** | **244 MiB** | 206 MiB |
| direct buffer | 27 MiB | — |

- JVM 커밋 총합 ≈ **1,255 MiB**, 스레드 스택·GC·JIT 네이티브까지 더하면 **약 1.3 GB = 호스트 RAM의 68%**
- `-Xms512m -Xmx1024m`(`Dockerfile:16`)에서 G1이 부하 중 상한까지 확장했고, 확장분은 쓰지 않아도
  즉시 반납되지 않아 그대로 스왑 대상이 된다
- 대조군: **같은 blue의 idle 커밋 힙은 542 MiB**(08-18 측정). 부하 때만 +440MB가 붙는 구조다

> **⚠️ 2026-08-19 정정 — 위 `used 337 MiB`는 11:24 한 시점 샘플이다.** R 구간(11:18~11:34)을
> `max_over_time`으로 다시 뜨면 **힙 used 피크는 601 MiB**이고, S 구간은 **652 MiB**다.
> 다만 그 대부분은 Eden 쓰레기라 GC로 압축되는 몫이고, 실제 요구량인 live set(Old Gen)은
> **R 341 / S 295 MiB**다. 5.6 조치 2의 근거를 이 값으로 교체했다 — 5.9 참고.

### 5.3 2순위 — Prometheus: k6가 검색 키워드를 라벨에 실어 보냈다

11:24 기준 **활성 시계열 158,716개**의 출처:

| 출처 | 시계열 수 |
|---|---|
| job 라벨 없음 = **k6 remote-write** | **151,911 (96%)** |
| postgres_exporter | 3,324 |
| backend | 2,470 |
| node-exporter ×2 (앱·DB) | 1,011 |

원인은 k6 태그다. 실제 시계열 라벨을 뽑으면 `name`/`url`이 **쿼리스트링까지 포함한 전체 URL**이다:

```
name = "https://newcodes.net/api/search/articles/loadtest?keyword=opentelemetry%20코루틴&page=1&size=10"
url  = (같은 값)
```

시나리오가 `tags: { endpoint: 'search', level }`만 주고 `name`을 덮어쓰지 않으며
(`scenarios/ramp-limit-finder.js:97`), `systemTags`로 `url`을 끄지도 않아 k6 기본값이 그대로 라벨이 된다.
키워드가 요청마다 다르니 **키워드 수 = 카디널리티**가 된다:

- `k6_http_req_duration_p95` 한 메트릭에 **3,452 시계열** (= 유니크 URL 수)
- k6 메트릭 이름 **56개** (`K6_PROMETHEUS_RW_TREND_STATS`의 통계 6종 × trend 8종 + 카운터/게이지)
- → **실행 1회당 15만~19만 시계열**. 게다가 `testid`가 매번 달라 실행끼리 절대 합쳐지지 않는다
- 최근 1시간(Q 10:38 + R 11:18) 창으로 세면 그 한 메트릭만 **11,755 시계열** →
  head 전체로는 **약 65만 시계열** 규모다 (stale 시계열도 2시간 블록이 잘릴 때까지 head에 남는다)

Prometheus 2.37의 head 시계열당 통상 3~4 KiB로 잡으면 **head+WAL만 대략 1~2 GB**다.
VU20에서 관측 스택이 통째로 죽고 재부팅 후 Prometheus가 큰 WAL을 재생하며 503을 거친 것과 맞는다.
같은 시각 **백엔드는 설정값대로 3.1초 Hikari 타임아웃을 정직하게 냈다**(2장) — 먼저 무너진 쪽은
백엔드가 아니라 Prometheus다.

누적 증거도 있다. 익명 총수요(anon + swap used)는 10:00 2,262 MiB → 11:20 2,525 MiB로 **+263 MiB**
늘었는데, 이 구간의 배포(green→blue 교체, 10:32)로 설명되는 몫은 +120 MiB뿐이다. 나머지는 Q 실행이
남긴 시계열이 head에 계속 머문 몫으로 본다.

### 5.4 메모리 장부 (11:24)

| 프로세스 | MiB | 근거 |
|---|---|---|
| **backend-blue JVM** | **~1,300** | **실측** (committed heap+nonheap+direct) |
| **Prometheus** | ~700–1,200 | **추정** — 시계열 수는 실측, 바이트/시계열은 통상값 |
| Tempo + Loki + promtail + Grafana | ~300–450 | 추정. `management.tracing.sampling.probability=1.0`이라 전 요청이 트레이싱된다 |
| nginx, exporter 3종, dockerd, 그 외 | ~150–200 | exporter RSS만 실측 (각 12~15 MB) |
| **합** | **~2.4–3.1 GB** | 실측 총수요 3.0 GB와 정합 |

### 5.5 이 분해의 한계와 확정 방법

**앱 호스트에는 cAdvisor도 process-exporter도 없어 컨테이너별 RSS가 Prometheus에 남지 않는다.**
위 표에서 JVM과 exporter만 실측이고, Prometheus·Tempo·Loki·Grafana는 추정이다. 확정하려면
운영 셸에서 아래를 남겨야 한다:

```bash
# 현재 상태(idle 기준선)
docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}"
smem -k -s swap -r | head -20        # 스왑까지 포함한 프로세스별 점유 (apt install smem)

# 다음 사다리 실행 때 같이 띄워둘 것 — 추정 칸이 실측으로 바뀐다
while true; do docker stats --no-stream --format '{{.Name}} {{.MemUsage}}' \
  | sed "s/^/$(date -u +%H:%M:%S) /" >> /tmp/memstat.log; sleep 10; done &
```

---

### 5.6 조치와 예상 절감

절감 추정의 전제: Prometheus 몫을 총수요에서 역산하면
`3,004 − JVM 1,300 − (Tempo·Loki·Grafana·nginx·시스템 450~650) ≈ **1,050~1,250 MiB**`이고,
이를 head 시계열 35만~65만으로 나누면 **시계열당 2~3 KiB** — Prometheus 2.x의 통상값과 맞는다.
아래 절감액은 이 역산에 기댄 추정이며, 5.5의 `docker stats` 측정으로 확정해야 한다.

| # | 조치 | 예상 절감 | 방법론 영향 |
|---|---|---|---|
| **1** | k6 `name`/`url` 라벨 제거 (`--system-tags`) | **700~1,100 MiB** (실행당 시계열 19만 → 500 미만, **−99.7%**) | ⚠️ p50/p95 산출식 교체 필요 (아래) |
| **1′** | `K6_PROMETHEUS_RW_TREND_STATS`를 `p(50),p(95),p(99)`로 축소 | ~350~500 MiB (메트릭 이름 56 → 32, −43%) | **없음** — `avg/min/max`는 소비자가 없다. `p(99)`는 대시보드 패널이 쓰므로 남긴다 |
| 2 | `-Xmx1024m` → `512m` | ~400 MiB (부하 중 커밋 힙 912 → 512) | ⚠️ **근거 교체(5.9).** ~~used 최대 337MB라 여유~~ → 실측 used 피크는 601(R)/652(S)이고, 512를 정당화하는 건 live set **300~341 MiB**다(여유 1.5배로 빠듯). 진짜 명분은 메모리 절감이 아니라 **GC 정지 제거**. **→ 5.11에서 하한 p99×2 = 506 / 상한 549로 재도출, 512 확정** |
| 3 | 컨테이너 `mem_limit` 부여 | (절감 아님) 호스트 전체 스래싱 대신 컨테이너 1개 OOM-kill로 국한하는 안전장치 | 없음 |
| 4 | `TRACING_SAMPLING=1.0 → 0.1` | ~~수십~150 MiB~~ → **5.7 실측 결과 Tempo가 544 MiB로 2위. 우선순위 2위로 상향** | 트레이스 표본 1/10 |

**1번만 반영했을 때**: 총수요 3,004 → **약 1,950~2,300 MiB**(RAM 1,906 대비 1.6배 → **1.0~1.2배**).
스왑 사용은 1,530MB → 200~400MB, swap-in은 2,500 pages/s → 수백 대로 떨어져
**관측 스택이 VU20을 넘길 가능성이 생긴다.** 다만 여유는 여전히 없다 —
**1번 + 2번을 같이 해야 총수요가 1,500~1,900 MiB로 RAM 안에 들어온다.**

**절감되지 않는 것**: 14.3 RPS 천장은 DB 제약이라 그대로다(1·4장). VU20의 5xx 48건도
수요 40 대 공급 5의 풀 문제라 그대로다. ingest 표본율도 원래 낮다(14 RPS × 56 메트릭 ≈ 800 samples/s) —
**이 조치의 절감은 CPU가 아니라 head 메모리와 인덱스다.** 부수 효과로 WAL이 작아져
재부팅 후 replay 시간(2장의 503 구간)도 함께 줄어든다.

> **⚠️ 1번의 숨은 의존성.** `collect-results.py:87`의 p50/p95는 **URL별 시계열을 요청 1건으로 간주해
> `quantile()`로 재구성**한다(코드 주석에 명시). `name`/`url`을 없애면 레벨당 시계열이 소수로 붕괴해
> 이 식이 무의미해진다 — 에러 없이 p50과 p95가 같은 값이 나오는, 가장 위험한 형태로 틀린다.
> **→ 2026-08-18 해결됨. 산출식 교체와 실측 캘리브레이션은 5.7 참고.**

### 5.7 조치 1+1′ 시행 (2026-08-18)

**적용 지점은 시나리오가 아니라 실행 레이어다** — `run-task.sh`/`run-local.sh`가 `--system-tags`와
`K6_PROMETHEUS_RW_TREND_STATS`를 넘긴다. 시나리오 11개를 안 건드리므로 **ECR 이미지 재빌드가 없고**,
`LT_SYSTEM_TAGS=`(빈 값) 한 번으로 즉시 롤백된다.

| 항목 | 값 |
|---|---|
| 유지 태그 | `status,method,error_code,check,group,scenario,expected_response,proto` |
| 제외 태그 | `url`, `name`, `error`, `tls_version`, `subproto`, `service` |
| TREND_STATS | `p(50),p(95),p(99)` (`avg,min,max` 제거) |
| 로컬 검증 | 방출 라벨에서 `url`/`name`/`error`/`tls_version` 소멸, `expected_response`·`endpoint`·`testid` 생존 확인 |

**산출식 교체 — 시간축 집계 함수를 실측으로 정했다.** k6 PRW의 trend sink는 flush 창별이 아니라
**시계열별로 누적**이다. R의 `k6_iteration_duration_p95{scenario="level_10"}`가
0.764 → 1.017 → 0.900 → … → **0.9497에서 수렴 후 고정**되는 것으로 확인했다(창 단위였다면 진동한다).
따라서 레벨 종료 시점 값이 곧 그 레벨 전체의 분위수이고, **`last_over_time`이 정답**이다 —
`avg_over_time`은 수렴 전 구간을 섞어 과소(−3%), `max_over_time`은 과도구간 피크를 집어 과대(+7%)다.

**캘리브레이션 — 신·구 차이는 ≤1.8%다.** 같은 실행(R) 안에서 두 방식을 모두 산출했다
(과거 실행은 `url` 라벨이 없는 `iteration_duration`으로 폴백 — ramp-limit-finder는 iteration당 요청 1회):

| VU | legacy p50/p95 | native p50/p95 | Δ |
|---|---|---|---|
| 5 | 356 / 499 | 354 / 490 | −0.6% / **−1.8%** |
| 10 | 589 / 959 | 595 / 950 | +1.0% / −0.9% |
| 15 | 908 / 1,536 | 908 / 1,536 | 0% / 0% |

**시계열 불연속은 사실상 없다.** 또한 legacy 모드가 이 문서 헤드라인 표의 네 레벨을 모두 정확히
재현하는 것으로 회귀 검증했다(VU20의 1,338 / 18,882 포함).

**부수 정정 2건**:
- 자동판별을 시계열 *개수*로 하면 VU20처럼 관측 스택이 죽어 데이터가 희박한 구간에서 오판한다 —
  **`url` 라벨의 유무**로 가르도록 고쳤다.
- 대시보드의 `avg(k6_http_req_duration_p*)` 패널 5개는 지금까지 URL별 수천 시계열의 평균이라
  자연스럽게 요청 수 가중이었는데, 라벨 축소 후에는 5건짜리 5xx가 2,500건짜리 2xx와 1:1로 평균된다 —
  `expected_response="true"` 필터를 쿼리에 추가했다.

#### 전환 실행 S (`20260818-075257`, VU 5/10/15)

**카디널리티 — 목표 초과 달성.**

| | R (08-17) | S (08-18) |
|---|---|---|
| 실행당 k6 시계열 | ~193,000 | **86** |
| Prometheus 전체 활성 시계열 | 158,716 | **9,512** |

수집 스크립트는 `native` 모드를 자동 판별했고 p50<p95 정상이다.

**5.4 장부 확정 — 추정을 실측이 갈아엎었다.** 앱 호스트 `docker stats`(08:04:06 UTC, 피크):

| 컨테이너 | 실측 MiB | 5.4의 추정 |
|---|---|---|
| newcodes-backend-blue | **684** (RSS) | ~1,300 (커밋 기준) |
| **tempo** | **544** | Loki·Grafana와 뭉뚱그려 300~450 |
| prometheus | **73** | 700~1,200 ❌ |
| grafana / loki | 48 / 42 | |
| exporter 3종 + nginx | 44 | 150~200 |
| **합** | **1,436** | |

- **Prometheus는 73 MiB로 무해해졌다** — 조치 1이 정확히 표적을 맞혔다(08:04:54의 204 MiB는 WAL/컴팩션 순간값).
- **5.4의 Prometheus 추정은 2배 이상 과대했다.** 158k 시계열이면 300~500 MiB가 현실적이고, 총수요에서
  역산할 때 차액을 전부 Prometheus에 몰아준 것이 오류였다. **그 차액의 실체는 Tempo다** —
  `management.tracing.sampling.probability=1.0`으로 전 요청이 트레이싱되고 `mem_limit`도 없다.
  조치 우선순위에서 **트레이싱 표본 축소가 4위 → 2위로 올라간다.**
- backend RSS 684는 커밋(heap 840 + nonheap 271 = 1,111)보다 작다 — `docker stats`는 상주분만 보여주고
  커밋·미접촉·스왑아웃 페이지는 안 잡는다. 호스트를 무너뜨리는 건 커밋 쪽이라 조치 2는 여전히 유효하다.

**호스트 — 관측 스택이 살아남았다.**

| | R 피크 | S 피크 |
|---|---|---|
| 총수요(anon+swap) | 3,004 MiB | 2,879 MiB |
| MemAvailable 최저 | **92 MiB** | **164 MiB** |
| GC 최대 정지 | 4.15s | 2.52s |
| 관측 스택 | VU20에서 사망 → 재부팅 | **정상** |

총수요가 125 MiB밖에 안 줄어든 것은 위 장부로 설명된다 — Prometheus에서 회수한 몫을 Tempo가
그대로 차지하고 있었고, 부하 종료 시점에 JVM 힙이 512 → 840 MiB로 확장했다.

**처리량 — 「천장 14.3 RPS 확정」이 흔들린다.**

| VU | R: 2xx / RPS / p95 / blks_req | S: 2xx / RPS / p95 / blks_req |
|---|---|---|
| 5 | 2,313 / 12.85 / 499 / 14,107 | 2,389 / 13.27 / 479 / 14,053 |
| 10 | 2,579 / 14.33 / 959 / 13,531 | 2,820 / **15.67** / 825 / 13,601 |
| 15 | 1,835 / 10.19 / 1,536 / **19,915** | 2,638 / **14.66** / 1,243 / **14,077** |

**VU15의 꺾임이 사라졌고 1장이 진단한 `blks/req +47%`가 재현되지 않았다**(14,077로 평평).
VU10은 15.67로 헤드라인의 천장 14.3을 넘었다. 다만 **이 개선을 조치 1의 공으로만 돌릴 수 없다** —
R 이후 호스트 재부팅이 있었고 S 실행 중 JVM 커밋 힙은 512 MiB였다(R은 984). 공통 원인은
**앱 호스트 메모리 압박 해소**이고, k6 몫과 재부팅 몫은 이 실행 하나로 분리되지 않는다.
유력해진 해석은 **1장의 `blks/req` 증가가 DB 자체 성질이 아니라, 앱이 느려져 DB 동시 요청이 쌓인
2차 효과**였다는 것이다. 헤드라인의 「14.3 = 이 빌드의 실제 천장」은 **메모리 압박 상태에서의 천장**으로
한정해 읽어야 한다 — 재측정이 필요하다.


### 5.8 Tempo 억제 (2026-08-18) — 544 MiB의 정체는 "필요량"이 아니라 GC 목표였다

5.7에서 Tempo가 실측 2위(544 MiB, 피크 RSS 625)로 드러났지만 **왜** 그만큼 쓰는지는 몰랐다.
`prometheus.yml`에 Tempo job이 없어 내부 지표가 남지 않았기 때문이다. Grafana 데이터소스 프록시로
`:3200/metrics`와 `/debug/pprof/heap`, `/status/config`를 직접 떠서 분해했다.

#### 실측 — 정상 상태 live heap은 85 MB뿐이다

| 지표 | 값 |
|---|---|
| `HeapAlloc` (GC 직후 live) | **85 MB** |
| `HeapInuse` / `HeapSys` | 95 MB / 818 MB |
| `HeapReleased` | 579 MB (이미 OS에 반납) |
| `NextGC` (다음 GC 목표) | 164 MB … **직전 관측 시점엔 738 MB** |
| `MaxRSS` (기동 후 피크) | **625 MiB** |
| `GCCPUFraction` | **0.0006 (0.06%)** |
| `go_goroutines` | 1,011 |
| 인제스터 live traces | **4개 / 6 KB** |
| 블록 수 / 누적 flush | 170 / 45개 (54 MB → 평균 **1.2 MB/블록**) |
| 22시간 누적 수신 | 326,755 spans / 257 MB |

**즉 Tempo는 544 MiB가 필요한 게 아니다.** 상주분의 대부분은 순간 버스트가 남긴 자국이다.

#### 원인 1 — GOGC=100 기본값이 버스트 기준으로 힙 목표를 2배로 잡는다

Go GC는 `GOMEMLIMIT`이 없으면 힙 목표를 **직전 live의 2배**로 잡는다. 블록 완성 중 live가 잠깐
370 MB까지 뛰면 목표가 738 MB가 되고, 그 사이 OS에서 받아온 페이지가 RSS로 눌러앉는다.
**GC CPU는 0.06%밖에 안 쓰고 있었다** — 메모리를 CPU로 바꿔 쓸 여유가 3자릿수 배로 남아 있었다.

#### 원인 2 — 힙 프로파일이 지목한 곳은 인제스터의 블록 완성 경로다

`inuse_space` 1위(41.4 MiB)와 `alloc_space` 1위(1,749 MiB)가 같은 스택이다:

```
parquet-go.newRowGroupRows ← FileRowGroup.Rows ← vparquet4.(*rowIterator).Next
  ← vparquet4.CreateBlock ← tempodb.CompleteBlockWithBackend ← ingester.(*instance).CompleteBlock
```

WAL 블록을 읽어 완성 블록으로 다시 쓰는 경로다. 뒤이어 `vparquet4.newRowPool`(1,007+288 MiB),
`BufferedWriter.Write`(494), `ZstdCodec.Encode`(321)까지 전부 같은 flush/compaction 축이다.
쿼리는 무관하다 — 기동 후 검색이 **6건**뿐이었다.

#### 원인 3 — 기본값이 이 호스트 규모와 3~100배 어긋나 있다 (`/status/config` 실측)

| 키 | 기본값 | 이 호스트의 현실 |
|---|---|---|
| `block.parquet_row_group_size_bytes` | **100 MB** | 실제 블록 평균 **1.2 MB** — 100배 과대 |
| `pool.max_workers` / `queue_depth` | **400 / 20,000** | 2 vCPU. goroutine 1,011개의 출처 |
| `search.read_buffer_count` × `size` | 32 × 1 MiB = **32 MiB/리더** | `max_workers`와 곱해진다 |
| `query_frontend.search.concurrent_jobs` | **1,000** | 블록당 잡 1개 → 170 블록 동시 조회 |
| `ingester.concurrent_flushes` | 4 | 완성 1건이 수십 MB |
| `global.max_bytes_per_trace` | 5 MB | 트레이스 1건이 힙을 밀 수 있다 |

> **부수 발견 — 아직 안 터진 폭탄이 하나 있다.** `pool.max_workers`(400) × `read_buffer_count`(32 MiB)는
> 상한이 없다. Grafana Explore에서 7일 범위 TraceQL 검색을 한 번 돌리면 이론상 수 GB를 잡는다.
> 지금까지 안 터진 건 검색이 6건뿐이었기 때문이다. RAM 1,906 MiB 호스트에서 이건 우연에 기댄 상태였다.

#### 조치

| # | 변경 | 파일 | 근거 |
|---|---|---|---|
| a | **`GOMEMLIMIT=300MiB`** | `docker-compose.yml` | 핵심 레버. 힙 목표에 천장을 씌운다. GC CPU 0.06% → 여유 충분 |
| b | `mem_limit: 400m` | `docker-compose.yml` | GOMEMLIMIT은 소프트다. 초과 시 호스트 스래싱 대신 컨테이너만 OOM-kill (5.6의 조치 3) |
| c | `parquet_row_group_size_bytes` 100 MB → **10 MB** | `tempo.yml` | 프로파일 1위 지점. 블록 크기에 맞춤 |
| d | `pool.max_workers` 400 → **20**, `queue_depth` 20,000 → 2,000 | `tempo.yml` | 2 vCPU |
| e | `search.read_buffer_count` 32 → **8** | `tempo.yml` | 위 폭탄의 뇌관 |
| f | `concurrent_jobs` 1,000 → 8, `target_bytes_per_job` 100 MB → 10 MB, `max_concurrent_queries` 20 → 4 | `tempo.yml` | 쿼리 경로 상한 |
| g | `concurrent_flushes` 4 → 1, `complete_block_timeout` 15m → 5m | `tempo.yml` | 완성 버스트 직렬화 |
| h | `max_bytes_per_trace` 5 MB → 1 MB | `tempo.yml` | 폭주 트레이스 방어 |
| i | `usage_report.reporting_enabled: false` | `tempo.yml` | 셀프호스트 단일 노드 |
| j | **Prometheus에 `tempo` job 추가** | `prometheus.yml` | 이번 진단에 pprof가 필요했던 이유. 시계열 ~1,000개로 저렴 |
| k | **k6가 `traceparent`(sampled=0)를 보낸다** | `load-test/lib/config.js` | 아래 |

`tempo.yml`은 `grafana/tempo:2.10.7 -config.verify=true`로 검증했다(오타 키를 넣으면 exit 1로
떨어지는 것까지 반증 확인).

#### 조치 k — `TRACING_SAMPLING=0.1` 대신 유입 지점에서 끈다

「다음」에 적어둔 건 `TRACING_SAMPLING=0.1`이었지만, 측정해 보니 **그건 틀린 레버다.**

- 22시간 요청 19,412건 중 부하테스트가 **7,775건(40%)**, 평시 트래픽은 11,637건 = **약 0.15 RPS**다
- 전역으로 0.1을 걸면 평시 트레이스가 **7분에 1건** 수준으로 떨어져 운영 관측성이 사실상 사라진다.
  정작 줄여야 할 버스트는 부하테스트 쪽인데 그건 10%만 줄어든다

백엔드는 커스텀 `Sampler` 빈이 없어 Boot 기본값 **`ParentBased(TraceIdRatioBased(1.0))`**를 쓴다.
따라서 **sampled 플래그가 0인 W3C `traceparent`가 들어오면 그 요청은 span을 하나도 만들지 않는다.**
`lib/config.js`의 `bypassHeaders()`(전 시나리오가 공유, `rate-limit-check.js`만 의도적으로 미사용)에
이 헤더를 붙였다. `LT_TRACE_RATIO`(기본 **0.02**)만큼은 헤더를 안 붙여 정상 트레이스로 남긴다 —
**워터폴 진단은 살리고 양은 1/50로 줄인다.** 운영 샘플링(1.0)은 손대지 않는다.

- `sampled=1`인 traceparent를 보내지 않는 이유: 존재하지 않는 부모를 가리켜 루트가 끊긴 트레이스가 된다
- 부작용: 억제된 요청도 MDC의 traceId는 남으므로 **Loki 로그의 traceId를 클릭하면 트레이스가 없다.**
  부하테스트 로그에 한정된다
- ⚠️ 이 변경은 `lib/`에 있어 **ECR 이미지 재빌드가 필요하다** (5.7의 조치 1과 달리 실행 레이어가 아니다).
  「다음」의 이미지 재빌드 항목과 함께 나가야 한다

#### 예상 효과와 검증 방법

`GOMEMLIMIT` 하나로 Tempo RSS는 **544 → 약 300 MiB**로 묶인다(하드 상한 400m). c~h가 순간 live를
낮추므로 재측정 후 **200MiB까지 조일 여지**가 있고, k가 들어가면 부하테스트 중 블록 완성 자체가
거의 사라진다. 총수요는 S 실행의 2,879 MiB에서 **약 2,630 MiB**로, 「다음」의 `-Xmx512m`까지 하면
**약 2,200 MiB**가 된다 — RAM 1,906은 여전히 넘으므로 **이 둘로도 스왑은 안 없어진다**.

> **2026-08-19 결과: 예상을 크게 초과 달성했다 — 평시 RSS 544 → 65 MiB (5.10).** 다만 아래 검증
> 방법은 두 군데가 틀렸다. `deploy.sh`는 관측 스택을 배포하지 않고, `prometheus.yml`은 bind mount
> inode 때문에 리로드가 아니라 **재기동**이 필요하다. 부하 중 거동은 여전히 미검증이다.

**검증 방법(원문)** — j의 스크레이프가 붙은 뒤 확인할 것:

```promql
process_resident_memory_bytes{job="tempo"}          # 목표: 300 MiB 미만
go_memstats_next_gc_bytes{job="tempo"}              # 목표: 300 MiB 근처에서 평평
rate(go_gc_duration_seconds_count{job="tempo"}[5m]) # GC 빈도 상승 폭 (CPU 대가)
```

`GOMEMLIMIT`이 너무 빡빡하면 Go는 OOM 대신 **GC를 계속 돌리며 CPU를 최대 50%까지 태운다**.
`GCCPUFraction`이 5%를 넘으면 상한을 400MiB로 완화할 것. 2 vCPU라 이건 실제 위험이다.

### 5.9 커밋 힙의 진짜 비용은 장부가 아니라 GC 정지다 (2026-08-19)

5.7 표의 「GC 최대 정지 4.15s → 2.52s」는 지금까지 원인 없이 떠 있던 한 줄이었다. 실측해 보니
**이 정지는 GC가 일을 많이 해서가 아니라, 스왑아웃된 힙 페이지를 STW 구간 안에서 폴트인하느라
생긴 것**이다. 조치 2(`-Xmx`)의 명분이 여기서 바뀐다 — 메모리 절감이 아니라 **지연 제거**다.

#### 메커니즘 — GC는 LRU에게 최악의 접근 패턴이다

1. `-Xmx1024m`이라 부하 중 G1이 **912 MiB까지 커밋**한다. 실제 live set은 **300 MiB 남짓**이라
   나머지 600 MiB는 커밋됐지만 한동안 아무도 안 만지는 페이지다.
2. 커널의 축출 기준은 "최근에 안 만진 익명 페이지" — 위 600 MiB가 정확히 그 대상이다.
   RAM 1,906에 관측 스택이 동거하니 실제로 스왑으로 나간다.
3. **그런데 그 페이지를 주기적으로 훑는 유일한 작업이 GC다.** Young GC조차 Eden/Survivor 스캔에
   더해 remembered set·card table을 따라 Old Gen 영역을 건드린다. 커널이 "차갑다"고 판단해
   내보낸 페이지를 GC가 정확히 되불러온다.
4. 폴트인은 디스크 랜덤 4KB 읽기이고, 이게 **STW 구간 안에서** 벌어진다. GC 스레드가 디스크를
   기다리는 동안 애플리케이션 스레드는 전부 정지다.

#### 실측 — 일감은 오히려 줄었는데 시간은 100배다 (S 실행, 08-18)

같은 실행 안의 같은 종류 GC(`G1 Young Generation` / `G1 Evacuation Pause`)다:

| 시각 (UTC) | GC 횟수/분 | 평균 정지 | 최대 정지 | swap-in (pages/s) |
|---|---|---|---|---|
| 07:50 (유휴) | — | — | **0.013s** | **1.7** |
| 07:58 | 9.3 | 0.030s | 0.228s | 428 |
| 08:00 | 6.7 | 0.023s | 0.052s | 60 |
| 08:02 | 9.3 | 0.029s | 0.137s | 368 |
| **08:03** | 9.3 | **0.589s** | **2.519s** | 205 |
| 08:04 | **4.0** | 0.015s | — | **958** |
| 08:05 | **1.3** | 0.233s | 1.398s | **1,136** |

- **정상 young GC는 23~56ms다** — 부하 내내 그렇다. 그런데 하나가 **2,519ms로 같은 실행 평균의 45~110배**다.
- **일감이 늘어서가 아니다.** young 대피 비용은 살아남은 객체 양에 비례하는데, 2.5초짜리가 터진
  08:03 시점 **Eden은 17 MiB**였다. 일감은 오히려 적다.
- **GC 횟수가 오히려 줄어든다**(9.3 → 4 → 1.3회/분). GC가 빨라진 게 아니라 JVM이 디스크를
  기다리느라 갇혀 있었다는 뜻이다.
- 유휴(07:50)와의 대조가 결정적이다 — 같은 힙·같은 콜렉터·같은 코드인데 **13ms vs 2,519ms(190배)**,
  달라진 변수는 swap-in **1.7 → 1,136 pages/s** 하나뿐이다.

#### 이것이 5xx로 이어지는 경로

2장 각주가 관측만 하고 원인을 연결하지 않았던 부분이다:

```
커밋 힙 912 (live 300) → 차가운 600 MiB 스왑아웃 → GC가 STW 안에서 폴트인
  → 4.15s 정지 (R) → 요청 스레드가 Hikari 커넥션을 쥔 채 정지
  → 대기 요청의 타임아웃 시계는 계속 흐름 → 3초 설정이 8.4초로 관측 → 5xx
```

문서는 이걸 "Q의 8.4초는 GC 탓, VU20의 3.1초는 순수 풀 초과"로 **분류만** 했다. 그 GC의 정체가
스왑이고 스왑의 정체가 커밋 힙이면, **`-Xmx` 한 줄이 그 8.4초의 뿌리**다.

#### 조치 2의 근거·기대효과 재작성

| 항목 | 종전 (5.6) | 교체 |
|---|---|---|
| 근거 | "used 최대 337MB라 여유 있다" | **live set 300~341 MiB** (used 피크 601/652는 대부분 Eden 쓰레기라 GC로 압축된다) |
| 여유 | 충분 | **1.5배로 빠듯** (G1 통상 권장 2배) |
| 기대효과 | 메모리 ~440 MiB 절감 | 스왑 가능한 차가운 힙 **400 MiB 소멸 → GC 폴트인 대상 자체가 없어짐**. 메모리 절감은 부산물 |
| 대가 | 없음 | young GC 횟수 증가(분당 7~9회 → 그 이상). 정상 1회가 30ms이고 CPU는 남으므로 2,519ms 하나와 바꾸면 압도적으로 남는다 |

**유휴 절감은 0이라는 점도 분명히 해둔다.** `-Xms512m`이라 현재 유휴 커밋이 이미 **525 MiB**다
(08-19 실측, used 250). Xmx만 512로 내리면 힙이 512에 고정되어 유휴 footprint는 그대로고,
절감은 **부하 중 912 → 512**에서만 생긴다. 유휴 상태에서도 스왑 사용 **880~990 MiB** /
MemAvailable **120~380 MiB**인 이 호스트의 만성 압박은 이 조치로 안 없어진다(5.8·6장과 같은 결론).

#### ⚠️ 방향이 뒤집힐 수 있는 조건 — 순서가 중요하다

스왑 압력이 그대로인 채 힙만 줄이면 **GC 횟수가 늘어 폴트인 기회도 같이 는다.** "힙이 작아
페이지가 따뜻하게 유지되는" 효과가 이기느냐 "횟수가 늘어" 지느냐는 호스트 여유에 달렸다.
→ **Tempo 억제가 실제로 먹은 것을 확인한 뒤** Xmx를 건드린다. 순서를 뒤집으면 개선이 아니라
악화일 수 있고, 무엇보다 어느 쪽이었는지 구분이 안 된다.

#### 시행 방법 — 이미지 재빌드가 필요 없다

`Dockerfile:19`가 shell form(`java $JAVA_OPTS -jar app.jar`)이라 compose `environment`의
`JAVA_OPTS`가 `Dockerfile:16`의 ENV를 덮는다. `DB_POOL_SIZE`/`DB_MAX_PARALLEL_WORKERS`와
**똑같은 실험 패턴**으로 넣고 재기동만 하면 A/B가 된다 (재빌드·이미지 푸시 없음).

```yaml
# docker-compose.yml, backend blue/green 공통 env
- JAVA_OPTS=${JAVA_OPTS:--Xms512m -Xmx1024m -XX:+UseG1GC -XX:MaxGCPauseMillis=200}
```

- 유휴 footprint까지 노리면 **`-Xms`도 같이 내려야 한다**(`-Xms256m -Xmx512m`). Xms를 512에 둔 채
  Xmx만 내리면 고정 힙이 되어 반납 여지가 사라진다
- 상한을 안 내리는 대안: `-XX:MaxHeapFreeRatio=30 -XX:MinHeapFreeRatio=10 -XX:G1PeriodicGCInterval=60000`으로
  **반납을 빠르게** 하고 1024는 안전판으로 남긴다. live 341에 512는 1.5배라 빠듯하므로 실패
  모드(풀 GC 폭주)는 이쪽이 더 안전하다
- 어느 쪽이든 tempo가 받은 `mem_limit`(5.6 조치 3)을 backend에도 붙여야 실패 시 호스트 전체
  스래싱 대신 컨테이너 하나로 국한된다

#### 검증 쿼리

```promql
max_over_time(jvm_gc_pause_seconds_max{cause="G1 Evacuation Pause"}[10m])   # 목표: < 0.2s
sum(jvm_memory_committed_bytes{area="heap"})/1024/1024                      # 목표: 512 근처 평평
rate(node_vmstat_pswpin{job="node-exporter"}[2m])                           # 목표: 피크 < 300 pages/s
sum(increase(jvm_gc_pause_seconds_sum[1m]))/sum(increase(jvm_gc_pause_seconds_count[1m]))  # 평균 정지 = 대가
```

---

### 5.10 Tempo 조치 배포 검증 (2026-08-19) — 메모리는 목표 초과 달성, 계측 배선은 실패

| 조치 | 상태 | 근거 |
|---|---|---|
| **a `GOMEMLIMIT=300MiB`** | ✅ | `docker inspect tempo --format '{{.Config.Env}}'` |
| **b `mem_limit: 400m`** | ✅ | `HostConfig.Memory` = 419430400 |
| **c~i (`tempo.yml` 8종)** | ✅ | `/status/config` 실측 (아래) |
| **j (Prometheus `tempo` job)** | ❌ | `up{job="tempo"}`이 없다 — 원인은 bind mount inode (아래) |

#### 메모리 — 544 → 65 MiB (−88%). 예상치 300 MiB를 크게 밑돈다

```
CONTAINER   NAME    CPU %    MEM USAGE / LIMIT     MEM %    PIDS
cbb2d43...  tempo   0.28%    65.43MiB / 400MiB     16.36%   23
```

`docker ps` 기준 **가동 21시간**이다(08-18 배포분). 조기 측정이 아니라 **정상 상태 값**으로 읽어도 되고,
21시간 동안 `mem_limit` 400m에 걸린 **OOM-kill이 한 번도 없었다**(uptime 연속).
PIDS 23은 5.8에서 goroutine 1,011개를 만들던 `pool.max_workers=400`이 20으로 내려간 결과와 정합적이다.

> **⚠️ 조건이 같지 않다.** 5.7의 **544 MiB는 S 부하테스트 피크** 시점 `docker stats`이고, 위 65 MiB는
> **평시 21시간**이다(평시 트래픽 0.15 RPS). 부하 중 블록 완성 버스트에서 어떻게 되는지는
> **다음 사다리 실행에서만 확정된다.** 다만 `mem_limit` 400m이 하드 상한이라 최악이어도
> 호스트 스래싱이 아니라 컨테이너 단독 OOM-kill로 국한된다 — 5.6 조치 3의 목적 그대로다.

#### `tempo.yml` c~i — `/status/config` 실측

| 키 | 기본값 | 현재 | 조치 |
|---|---|---|---|
| `storage.trace.block.parquet_row_group_size_bytes` | 100 MB | **10 MB** | c ✅ |
| `storage.trace.pool.max_workers` / `queue_depth` | 400 / 20,000 | **20 / 2,000** | d ✅ |
| `storage.trace.search.read_buffer_count` | 32 | **8** | e ✅ |
| `query_frontend.search.concurrent_jobs` / `target_bytes_per_job` | 1,000 / 100 MB | **8 / 10 MB** | f ✅ |
| `querier.max_concurrent_queries` | 20 | **4** | f ✅ |
| `ingester.concurrent_flushes` / `complete_block_timeout` | 4 / 15m | **1 / 5m** | g ✅ |
| `overrides.defaults.global.max_bytes_per_trace` | 5 MB | **1 MB** | h ✅ |
| `usage_report.reporting_enabled` | true | **false** | i ✅ |
| `compactor.compaction.block_retention` | — | 168h (의도대로 유지) | — |

#### 조치 j 실패 — 원인은 **bind mount inode**이고, nginx 함정과 같은 것이다

증상이 모순처럼 보였다:

```
06:23:51  Prometheus "Completed loading of configuration file"   ← SIGHUP 리로드 성공
호스트 ~/small-town/prometheus.yml 에 job_name: 'tempo' 존재    ← 파일도 정상
06:30     up{job="tempo"} 여전히 없음                             ← 그런데 타깃이 없다
```

**실측으로 확정했다** — 컨테이너가 보는 파일과 호스트 파일이 다른 파일이다:

```bash
$ grep -c tempo ~/small-town/prometheus.yml                         # 호스트
3
$ docker exec prometheus grep -c tempo /etc/prometheus/prometheus.yml   # 컨테이너
0
```

**Prometheus가 읽은 파일이 그 파일이 아니다.** `docker-compose.yml:154`는 `./prometheus.yml`을
**단일 파일 bind mount**하는데, 파일 마운트는 경로가 아니라 **inode를 묶는다.** `git pull`은 제자리
수정이 아니라 임시 파일 + rename이라 **inode가 교체된다** → 컨테이너는 계속 옛 inode를 본다.
SIGHUP은 그 **옛 내용을 성공적으로 다시 읽었다.** `docker ps` 기준 prometheus는 **2주 전 생성**이고
마지막 기동이 43시간 전(08-17 호스트 재부팅)이라, 그 사이 모든 `git pull`이 마운트에서 떨어져 나갔다.

**CLAUDE.md에 이미 적혀 있는 nginx 함정과 정확히 같은 메커니즘이다** —
*"Nginx 설정 변경 시 `sed -i` 금지 (inode 교체 → `docker restart newcodes-nginx` 필요)"*.
그게 nginx 전용 주의사항이 아니라 **bind mount된 모든 설정 파일의 문제**였다.

이 설명은 **왜 tempo만 반영됐는지도 설명한다** — tempo는 compose 정의(`GOMEMLIMIT`/`mem_limit`)가
바뀌어 **컨테이너가 재생성**되면서 마운트가 새 inode로 다시 걸렸고, prometheus는 compose 정의가
안 바뀌어 재생성 대상이 아니었다. 조치 a·b가 c~i를 데리고 들어온 셈이다.

```bash
docker restart prometheus     # 마운트를 현재 inode로 다시 건다. SIGHUP으로는 영원히 안 된다
```

> **리로드 vs 재기동 판정법**: 설정 *내용*만 바뀌었고 파일을 **제자리 수정**했다면 SIGHUP으로 충분하다.
> `git pull`·`sed -i`·에디터 저장처럼 **파일이 교체(rename)됐다면 재기동**이어야 한다.
> 판별이 애매하면 위처럼 **호스트와 컨테이너에서 같은 grep을 돌려 대조**하는 게 가장 빠르다 —
> 리로드 로그의 "성공"은 이 실패를 못 잡는다.

#### 배포 파이프라인의 진짜 문제

`deploy.sh`가 하는 일은 백엔드 blue/green 교체(`docker compose up -d newcodes-backend-blue`)와
nginx 재기동뿐이고 `prometheus`/`tempo`는 대상이 아니다(grafana만 별도 경로에서 `up -d`).
5.8에 적어둔 **"`./deploy.sh deploy` 후 확인"이라는 지침 자체가 틀렸다.**

여기에 inode 문제가 겹치면 실패 모드가 **조용하다**:
compose 정의가 바뀐 서비스는 재생성되며 설정이 따라 들어가고, 정의가 안 바뀐 서비스는
파일만 갈아입은 채 **옛 설정으로 계속 돈다 — 로그에는 "리로드 성공"이 찍힌다.**
관측 스택 변경 절차를 `deploy.sh`에 넣든 별도로 문서화하든 정해두지 않으면 이 조합이 반복된다.

### 5.11 `-Xmx` 사이징 — 512는 하한과 상한이 만나는 유일한 값이다 (2026-08-20)

5.9는 "512로 내린다"고 정했지만 근거는 **live set 300~341에 대한 1.5배**라는 한 줄이었고, 스스로
"빠듯하다"고 적어뒀다. 4일치(08-16~08-20) Prometheus를 직접 떠서 **하한(GC 여유)과 상한(호스트 예산)을
각각 독립으로 계산**했다. 두 구간이 **[506, 549] MiB**에서 겹치고, 그 안의 관례값은 512뿐이다.
1.5배가 아니라 **p99 기준 2.0배**가 512를 가리킨다 — 근거가 바뀐 게 아니라 **강해졌다.**

> 아래 수치는 별도 표기가 없으면 `job="backend"` 4일 창(08-16 13:00 ~ 08-20 11:00 UTC) 실측이다.
> blue/green 두 인스턴스를 따로 적는다 — 배포 교체로 둘 다 활성 구간이 있어 합산하면 겹친다.

#### 5.11.1 최악의 GC 정지는 부하테스트가 아니라 **매일 04:07 크롤링 창**이다

`max_over_time(jvm_gc_pause_seconds_max[30m]) > 2`를 4일 전체에 걸어 뽑은 목록이다:

| 시각 (KST) | 정지 | 인스턴스 | 정황 |
|---|---|---|---|
| 08-17 **04:07 / 04:37** | **9.591s** | blue | 크롤링 |
| 08-17 17:07 | 4.347s | green | — |
| 08-17 20:07 | 4.152s | blue | **런 R** |
| 08-17 22:07 | 3.374s | blue | — |
| 08-18 **04:07** | **10.893s** | blue | 크롤링 |
| 08-18 17:07 | 2.519s | blue | **런 S** |
| 08-19 **04:07 / 04:37 / 05:07** | **13.294s / 21.602s / 2.033s** | green | 크롤링 |
| 08-20 **04:07 / 04:37** | **12.321s / 16.956s** | green | 크롤링 |

`CrawlingScheduler`는 **04:00 블로그 / 04:30 유튜브 / 05:00 본문 백필**(KST)이다. 04:07·04:37·05:07이
분 단위로 여기에 붙는다. **부하테스트 최악(4.152s)의 5.2배가 매일 밤 자동으로 재현되고 있었다.**

압력원은 k6가 아니라 **같은 컨테이너에서 뜨는 headless Chrome**이다 — `Dockerfile`이
`google-chrome-stable`을 설치하고 `WebDriverConfig`가 로컬 드라이버를 띄운다(원격 그리드가 아니다).

**그리고 이 창의 실측이 `-Xmx` 논거를 확정한다.** 16.956초가 난 08-20 04:00~06:00 KST 창:

| 시각 (KST) | live set | 힙 used (30분 max) |
|---|---|---|
| 03:30 (HN) | 161.7 MiB | 453.0 MiB |
| **04:00 (블로그)** | 165.0 | 474.6 |
| **04:30 (유튜브)** | 178.3 | 359.7 |
| **05:00 (본문 백필)** | 180.5 | 481.0 |
| 05:30 | 178.4 | **510.0** |
| 06:00 | 173.5 | 501.9 |

**live 180인데 커밋 힙은 900+였다.** 720 MiB가 "커밋됐지만 아무도 안 만지는 페이지"였고, 5.9의
메커니즘 그대로 스왑아웃 → STW 안에서 폴트인됐다. 뒤집으면: **이 창의 실제 수요(used 피크 510)는
512 안에 그대로 들어온다.** 512로 내려서 잃는 것이 없고 얻는 것이 16.956초다.

> ⚠️ **시각 해상도의 한계.** `jvm_gc_pause_seconds_max`는 Micrometer의 3분 슬라이딩 최대이고
> 위 표는 30분 버킷의 `max_over_time`이다. 따라서 "04:07"은 **±30분 버킷 + 3분 윈도**의 값이지
> 정지가 일어난 초 단위 시각이 아니다(08-17 04:07과 04:37에 같은 9.591이 뜨는 것이 그 증거다).
> 크롤링 스케줄과의 대응은 **버킷 수준에서** 성립한다. 초 단위로 묶으려면 6장이 지적한
> `-Xlog:gc*,safepoint`가 필요하다.

#### 5.11.2 하한 — live set 분포 (실측)

`jvm_gc_live_data_size_bytes`는 old gen 수집 직후의 live 크기다. **used가 아니라 이게 요구량이다.**

| 통계 | blue | green |
|---|---|---|
| 중앙값 | 175.9 MiB | 172.9 MiB |
| **p99** | **238.6** | **253.5** |
| 최대 | 279.4 | **337.4** |
| 300 MiB 초과 시간 | **0분** | **12분 / 5,760분 (0.2%)** |
| old gen used 최대(부유 쓰레기 포함) | 341.1 | 373.3 |

5.9가 근거로 쓴 **337은 4일 중 12분짜리 단발**이다. 여유 배수를 최대값이 아니라 p99로 잡는 것이
맞고(그 12분은 힙이 잠깐 1.5배로 좁아질 뿐 Full GC가 아니다), 그러면:

```
하한 A (GC 여유 관례) = p99 253.5 × 2.0배(G1 통상 권장) = 506 MiB
하한 B (절대 하한)    = old gen 실측 최대 341 + 최소 Eden(G1NewSizePercent=5%) 26 = 367 MiB
```

B 아래는 OOM 영역이고, A가 실용 하한이다.

#### 5.11.3 상한 — 호스트 예산에서 역산

| 항목 | MiB | 근거 |
|---|---|---|
| MemTotal | 1,906 | 실측 |
| − tempo | 83 | **실측** (`process_resident_memory_bytes`, 조치 j 배선 후) |
| − prometheus | 73 | 실측 (5.7) |
| − loki + promtail + grafana | ~250 | **추정** — 스크레이프 없음 |
| − nginx + exporter 3종 + dockerd + 그 외 | ~170 | 부분 실측 (exporter 각 12~14) |
| − 커널 slab + pagetables | ~120 | 실측 (5.1의 98 + 18) |
| − 페이지캐시 최소분 | ~200 | 현재 Cached 295. 5.1의 117까지 쪼그라들면 파일 I/O가 스래싱한다 |
| **= 백엔드 JVM 몫** | **~1,010** | tempo 버스트(392) 시 **~701** |

```
상한 C = 1,010 − nonheap 274 − direct 27 − 네이티브 160(추정) = 549 MiB
```

**[506, 549] 안의 관례값은 512뿐이다.** 640은 상한을 105 초과하고, 384는 하한 A를 122 미달한다.

#### 5.11.4 Xmx별 JVM 총 커밋

`heap + nonheap 274 + direct 27 + 네이티브(GC 구조 ≈ 힙의 10% + 스레드 50 + JIT/malloc 60)`.
**네이티브 항은 추정이다** — 확정은 5.11.9 참고.

| Xmx | 총 커밋 | 예산 1,010 대비 | 판정 |
|---|---|---|---|
| 384m | 833 | −177 | 하한 A 위반 |
| **512m** | **974** | **−36** | ✅ |
| 640m | 1,115 | +105 | ❌ |
| 768m | 1,256 | +246 | ❌ |
| **1024m (현재)** | **~1,530** | **+520** | ← 매일 스왑으로 나가는 양 |

마지막 줄이 5.1의 실측과 맞는다. 유휴 상태의 지금도 익명 총수요는
**AnonPages 1,244 + swap used 903 = 2,147 MiB = RAM의 1.13배**다(MemAvailable 302, Cached 295).
**부하가 없어도 초과다.**

#### 5.11.5 「used 피크 800」은 사이징 근거가 아니다

5.9의 정정(“601/652”)조차 낮게 잡은 값이다 — 4일 창의 힙 used 최대는 **blue 800.0 / green 793.3**이고,
부하가 없는 08-20 17:37 KST에도 **758**이 뜬다. 그런데 이건 수요가 아니다:

- **Eden committed 최대가 574 MiB**다(= 1024의 56%, `G1MaxNewSizePercent=60`의 기본 동작).
  G1은 힙이 남으면 Eden을 상한까지 키우고 **다 찰 때까지 수집하지 않는다.**
- 따라서 `used 800 = live 180~250 + 쓰레기 550~620`이고, 이건 "필요량"이 아니라 **"상한을 준 결과"**다.
- 복사형 수집기의 1회 비용은 **살아남은 객체 양**에 비례하지 쓰레기 양에 비례하지 않는다.

두 지표가 서로를 검증한다:

```
19.8 GC/분(피크) × Eden 574 MiB ÷ 60초 = 189 MiB/s
실측 할당률 피크                          = 173 MiB/s     ← 8% 이내로 일치
```

#### 5.11.6 대가의 정량 — GC는 얼마나 더 도는가

| | 현재 (Xmx1024) | 예상 (Xmx512) |
|---|---|---|
| Eden 상한 | 574 MiB | **307 MiB** (512 × 60%) |
| young GC 빈도(피크) | 19.8회/분 | **~34회/분** (173 MiB/s ÷ 307) |
| 1회 정지 | 23~56ms (5.9의 정상 구간 실측) | 동일 — 생존량이 안 변한다 |
| **STW 총량(피크)** | 0.6~1.1 s/분 | **~1.0 s/분 = 1.7%** |
| 4일 major GC | **0회** | 0회 유지가 판정 기준 |

2 vCPU에서 1.7%는 지불 가능하고, 그걸로 **21.602초 정지 하나**와 바꾼다.

**솔직한 부작용**: Eden이 작아지면 객체가 죽을 시간이 줄어 생존율·승격이 오른다. 다만 현재
승격률 피크가 **0.48 MiB/s**라 2배가 돼도 1 MiB/s다.

참고로 4일 young GC는 **4,119회**이고 그중 **884회(21%)가 `G1 Humongous Allocation` 유발**이다.
리전은 512m에서도 1MB(최소값)라 humongous 임계(512KB)는 그대로다. `G1HeapRegionSize`를 올리면
임계는 오르지만 512m에서 리전이 256개로 줄어 수집 단위가 너무 굵어진다 — **건드리지 않는다.**

#### 5.11.7 512에서 old gen이 터지는가 — 계산상 아니다

```
헤드룸        = 512 − 341(old gen used 실측 최대) = 171 MiB
승격률 피크    = 0.48 MiB/s
171 ÷ 0.48    = 356초 ≈ 6분        ← 동시 마킹(STW 아님)은 초 단위에 끝난다
```

여기에 **4일간 major GC 0회**(`action="end of major GC"` 시계열 자체가 없다)가 더해진다.
5.9가 "1.5배로 빠듯"이라 부른 실패 모드는 이 데이터에서 관측되지 않았다.

#### 5.11.8 확정 옵션 세트 — **적용 완료 (2026-08-20)**

```yaml
# docker-compose.yml:30  &backend-env 앵커 — blue/green 동시 적용, 이미지 재빌드 없음
- JAVA_OPTS=${JAVA_OPTS:--Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:MaxMetaspaceSize=384m -XX:MinHeapFreeRatio=10 -XX:MaxHeapFreeRatio=30 -XX:G1PeriodicGCInterval=60000 -XX:+ExitOnOutOfMemoryError}
```

적용 전 compose에는 `JAVA_OPTS`가 **아예 없어서** `Dockerfile:16`의 `-Xms512m -Xmx1024m`이 그대로 살아
있었다(`jvm_memory_max_bytes{area="heap"}` = 1024로 확인). `docker compose --profile green config`로
blue/green **둘 다** 주입되는 것을 확인했다.

| 옵션 | 이유 |
|---|---|
| `-Xms256m` | **이게 없으면 유휴 절감이 0이다**(5.9). Xmx만 내리면 512 고정 힙이 된다 |
| `MinHeapFreeRatio=10 / MaxHeapFreeRatio=30` + `G1PeriodicGCInterval=60000` | JDK 12+ G1은 concurrent cycle에서 uncommit한다 — 유휴 힙이 실제로 OS로 돌아간다. live 180 기준 목표 커밋 = 180/(1−0.30) ≈ **257 MiB** |
| `MaxMetaspaceSize=384m` | 4일 피크가 **Metaspace 201.4 + CCS 27.1 = 228**이다. 상한이 무제한인 것을 막되 1.7배 여유를 둔다 |
| `+ExitOnOutOfMemoryError` | `restart: unless-stopped`가 복구하게 한다. 반쯤 죽은 JVM이 헬스체크만 통과하는 상태를 막는다 |
| `-Xss` 조정 | **불필요.** `spring.threads.virtual.enabled=true`라 플랫폼 스레드 peak가 61(blue)/75(green)뿐이다 |
| `+AlwaysPreTouch` | **금물.** 커밋을 앞당겨 스왑을 키운다 |

> ⚠️ **적용 시점 정정 2건.** 초안의 `MaxMetaspaceSize=256m`과 `MaxDirectMemorySize=64m`은 둘 다
> 근거가 부족해 바꿨다.
>
> - **`MaxMetaspaceSize` 256 → 384.** 256은 *현재값* 179만 보고 잡은 수였다. 4일 피크는 green의
>   **201.4**이고 CCS 27.1을 더하면 228이라 256은 1.12배밖에 안 된다. `+ExitOnOutOfMemoryError`와
>   겹치면 배포 한 번에 클래스가 조금 늘어도 **crash-loop**가 된다.
> - **`MaxDirectMemorySize=64m`은 뺐다.** 근거로 쓴 `jvm_buffer_memory_used_bytes{id="direct"}`
>   (피크 27.3)는 `ByteBuffer.allocateDirect`만 센다. **Netty의 `allocateDirectNoCleaner` 경로는
>   이 지표에 안 잡히는데**, `BedrockClientFactory`가 `NettyNioAsyncHttpClient`로 RAG 스트리밍을
>   돌린다(`software.amazon.awssdk:netty-nio-client`). 즉 27.3은 하한이지 피크가 아니고, 64로 묶으면
>   RAG 부하 중 `OutOfDirectMemoryError`가 날 수 있는데 **그걸 배제할 측정치가 없다.**
>   게다가 이 옵션은 절감이 아니라 가드일 뿐이고, **기본값이 Xmx라 Xmx를 내리는 것만으로
>   상한이 1024 → 512로 따라 내려간다** — 새 위험 없이 가드를 얻는다.
>   직접 묶으려면 `-Dio.netty.maxDirectMemory` 또는 NMT로 실제 direct 사용량을 먼저 재야 한다.

#### 5.11.9 한계 — 이 계산에서 추정인 것과, 답을 뒤집는 조건

- **`loki`·`grafana`·`promtail`은 스크레이프가 없어 250 MiB가 추정이다.** 예산 1,010의 오차가 여기서
  온다. tempo(조치 j)처럼 붙이거나 5.5의 `docker stats` 루프를 크롤링 창까지 돌려 확정할 것.
- **네이티브 160 MiB도 추정이다.** `-XX:NativeMemoryTracking=summary`로 한 번 확정하면
  5.4 장부의 마지막 추정 칸이 사라진다(오버헤드 1~2%).
- **live p99가 300을 넘기 시작하면 하한 A가 600이 되어 512는 무효다.** Caffeine `aiSummary`,
  `SearchQueryEmbedding` 캐시는 트래픽·데이터와 함께 자란다 — 월 단위 감시 항목이다.
- **512여도 tempo 버스트(392) 순간에는 ~270 MiB 초과한다.** `max_block_duration` 30m→10m가
  미적용인 것이 이 때문에 중요하다(7.2). 관측 스택 동거 구조 자체는 6장 결론 그대로다.
- **backend `mem_limit`(5.6 조치 3)은 보류한다.** 크롤링 창에 Chrome이 같은 컨테이너에서 뜨므로
  JVM ~974 + Chrome 200~400이 되어, 안전장치가 아니라 **매일 밤 OOM-kill 예약**이 된다.
  04:00~05:30 KST에 컨테이너 RSS를 먼저 재고 그 위에서 정할 것. 같은 이유로
  `mem_swappiness: 0`(스왑 폴트인을 근본적으로 없애는 유일한 수단)도 예산이 실제로 RAM 안에
  들어온 뒤에 건다 — 지금 걸면 정지가 OOM-kill로 바뀔 뿐이다.

#### 검증 — 판정 창은 부하테스트가 아니라 **04:00~05:30 KST**다

5.11.1이 보인 대로 이 조치의 최대 효과는 크롤링 창에서 난다. 사다리로만 재면 5.2배를 놓친다.

```promql
sum(increase(jvm_gc_pause_seconds_count{action="end of major GC"}[1h]))    # 0이어야 한다. 1↑면 512 부족
max_over_time(jvm_memory_usage_after_gc[1h])                               # > 0.7 지속이면 640으로
max_over_time(jvm_gc_pause_seconds_max{cause="G1 Evacuation Pause"}[10m])  # 목표 < 0.2s (현재 크롤링 창 16.956)
max_over_time(jvm_gc_overhead[1h])                                         # 목표 < 0.02 (현재 최대 0.075/0.106)
max_over_time(sum(jvm_memory_committed_bytes{area="heap"})[1h:1m])/1024/1024  # 512 근처 평평
quantile_over_time(0.99, jvm_gc_live_data_size_bytes[1d])/1024/1024        # 하한 A의 감시값. 300 넘으면 재계산
```

롤백은 `.env`에서 `JAVA_OPTS=`를 지우고 재기동 — 이미지 무관, `DB_POOL_SIZE`와 같은 패턴이다.

### 5.12 적용 결과 (2026-08-21) — 메커니즘은 확증됐고, 우리가 넣은 옵션 하나가 이득의 절반을 까먹었다

배포는 **08-20 20:37 KST**(green, `jvm_memory_max_bytes{area="heap"}` = 512 확인)이고, 그날 밤
크롤링 창이 새 설정으로 돌았다. **판정 창은 5.11이 정한 대로 04:00~05:30 KST 90분**이며,
직전 3일 밤과 같은 창·같은 쿼리로 비교했다.

| 크롤링 창 90분 | 08-20 (Xmx1024) | **08-21 (Xmx512)** | |
|---|---|---|---|
| 커밋 힙 (유휴 / 크롤링 피크) | 543 / 900+ | **276 / 486** | ✅ 예측 257과 일치 |
| **G1 Evacuation STW** | 50.198s (100.3회) | **25.553s (128.4회)** | ✅ **−49%** |
| G1 Humongous STW | 1.813s (31.1회) | 2.871s (46.1회) | ⚠️ 악화 |
| **G1 Periodic STW** | — | **18.344s (28.1회)** | ❌ **우리가 만든 것** |
| STW 합계 | 52.011s | 46.768s | **−10%뿐** |
| 최대 정지 | 16.956s | **15.877s (Periodic)** | ❌ |
| major GC | 0회 | **0회** | ✅ |
| swap used 최대 | 1,813.6 MiB | 1,656.7 | ✅ −157 |
| MemAvailable 최저 | 159.8 | 133.8 | ~ |
| swap-in 피크 | 2,111 pages/s | **2,335** | ❌ +11% |

유휴 대조(08-20 20:32 → 08-21 07:25, 둘 다 무부하):

| | 적용 전 | 적용 후 |
|---|---|---|
| 커밋 힙 | 543 MiB | **276** |
| MemAvailable | 301.8 | **494.8** |
| AnonPages | 1,243.8 | 1,072.3 |
| swap used | 902.8 | 956.8 |
| **익명 총수요** | **2,146.6 (RAM의 1.13배)** | **2,029.1 (1.06배)** |

#### 5.12.1 5.9의 인과가 확증됐다 — 이보다 깨끗한 증거는 없다

**Evacuation 정지 시간이 절반이 됐는데 횟수는 28% 늘었다**(100.3 → 128.4회).

```
1회당 비용:  50.198/100.3 = 0.502s  →  25.553/128.4 = 0.199s   (−60%)
```

GC가 일을 덜 해서 빨라진 게 아니다. 일감(생존 객체)은 그대로이고 **횟수는 오히려 늘었는데 1회 비용만
떨어졌다.** 5.9가 세운 "폴트인할 차가운 페이지가 없어진다" 말고 이걸 설명하는 가설이 없다.
**예측한 대가(GC 횟수 +28%)를 정확히 치르고 예측한 이득을 받았다.**

#### 5.12.2 그런데 `G1PeriodicGCInterval=60000`이 18.344초를 새로 만들었다

- 그날 밤 **최악의 정지 15.877초는 `G1 Periodic Collection`** — 5.11.8에서 우리가 넣은 옵션이 원인이다
- 1회 평균 **0.653초**로 같은 창 Evacuation(0.199초)의 **3.3배**다. 당연하다 —
  **이 GC는 "60초간 GC가 없었던" 시점, 즉 페이지가 가장 차가워진 시점에만 발동한다**
- 이게 없었다면 STW 합계는 46.768 → **28.42초**, 08-20 대비 **−45%**였다

비용은 시간대에 완전히 쏠려 있다(가동 10.8시간 전체 기준, Periodic 합 21.728s):

| 구간 | Periodic STW | 시간당 |
|---|---|---|
| 크롤링 창 1.5시간 | **18.344s** | **12.2 s/h** |
| 나머지 9.3시간 | 3.384s | **0.36 s/h** |

**크롤링 창 밖에서는 사실상 공짜이고, 유휴 커밋 543 → 276을 벌어준 것이 바로 이 옵션이다.**
따라서 끄는 게 아니라 **부하 중에만 막는 것**이 맞는 조치다.

#### 5.12.3 조치 — `-XX:G1PeriodicGCSystemLoadThreshold=1`

이 플래그는 주기 GC가 발동할 시각에 1분 load average를 보고 **임계를 넘으면 그 회차를 취소**한다
(기본 0 = 검사 안 함 = 무조건 실행, 지금까지의 동작).

`node_load1` 실측이 두 상태를 가른다:

| 상태 | load1 |
|---|---|
| 유휴 (9시간 중앙값) | **0.04** |
| 03:30 / 04:00 (크롤링 직전) | 0.31 / 0.33 |
| — 임계 **1** — | *실측값이 하나도 없는 구간* |
| 05:30 / 05:00 / 04:30 (크롤링 중) | 1.56 / 3.69 / **17.98** |

두 무리 사이가 0.33 ↔ 1.56으로 **4.7배 벌어져 있고 그 틈에 표본이 없다.** 2 vCPU의 절반이라는
의미도 자연스럽다. 적용 후 밤의 흐름:

```
03:30  load 0.31  → 실행 O   힙 반납 (정지 10~30ms)
04:05  load 17.98 → 취소 X   ← 15.877초가 여기서 사라진다
05:00  load 3.69  → 취소 X
05:30  load 1.56  → 취소 X
06:00  load 0.23  → 실행 O   ← 크롤링으로 부푼 힙(486)을 여기서 반납
```

**잃는 것이 없다.** 크롤링 중엔 힙을 실제로 쓰는 중이라 반납할 것도 없고, 반납이 06:00으로
한 시간 미뤄질 뿐이다. 유휴 −267 MiB는 유지된다.

- **예상**: 크롤링 창 STW 46.8 → **약 28초**(08-20 대비 −45%), 최대 정지는 Periodic이 빠지고
  Evacuation 계열(11.549초)로 내려온다
- **검증 가능한 부수 예측**: swap-in 피크가 함께 내려가면(2,335 → ?) **그 폴트인의 일부가
  주기 GC 자신이 만든 것**이었다는 뜻이다. 안 내려가면 스왑 압력은 순수하게 Chrome 몫이다

#### 5.12.4 확인된 부작용과 남은 것

- **Humongous 유발 GC가 31.1 → 46.1회(+48%), 시간 1.813 → 2.871초.** 리전 수가 1,024 → 512개로
  줄어 512KB 이상 객체가 연속 리전을 찾는 빈도가 는 것으로 본다. 5.11.6이 "감시만 하고 리전 크기는
  건드리지 않는다"고 한 판단은 유지 — 절대량이 2.9초로 작고, `G1HeapRegionSize`를 올리면 512m에서
  리전이 256개로 너무 굵어진다
- **major GC 0회** (11시간, `end of minor GC` 860회만). **512는 부족하지 않다** — 5.11.7의 계산대로다
- **swap-in 피크는 오히려 +11%(2,111 → 2,335 pages/s)**. 커밋 힙을 400 MiB 줄였는데 호스트의 폴트인
  총량은 안 줄었다. 5.12.3의 부수 예측이 이걸 가른다
- **익명 총수요 2,029 > RAM 1,906.** 여전히 초과다. Evacuation 11.5초가 남는 이유이고,
  다음 레버는 5.11.9 그대로 Tempo `max_block_duration` 30m→10m와 관측 스택 분리다

#### 5.12.5 이번 검증이 방법론에 남긴 것

**옵션을 묶어서 넣으면 어느 것이 무슨 일을 했는지 못 가른다** — 이번에는 `jvm_gc_pause_seconds_*`가
`cause` 라벨을 갖고 있어서 사후에 갈렸다(Evacuation −49% / Periodic +18.3s). 라벨이 없었다면
"STW 합계 −10%"만 보고 **Xmx 조치가 미미했다고 잘못 결론 내렸을 것이다.** 조치를 묶을 때는
**결과를 조치별로 분해할 라벨이 있는지 먼저 확인**한다.

---

## 6. 한계

- **VU20 구간의 원인 분해가 불가능하다** — 그 시각 DB/호스트 지표가 없다. 풀 포화는 앱 로그로
  확인했지만, 호스트 메모리가 얼마나 기여했는지는 측정되지 않았다.
- R은 1회 실행이다. 다만 정점 값은 Q와 교차 확인됐다.
- 실행 전부터 스왑 압박(SwapFree 445MB)이 있었으므로 **VU5의 12.85 RPS는 Q의 VU5(10.92)와
  직접 비교하기 어렵다.** 레벨 순서도 다르다(첫 레벨 vs 세 번째 레벨).
- **5.4의 컨테이너별 배분은 추정이었고 5.7에서 `docker stats` 실측으로 갈아엎였다** — Prometheus는
  2배 이상 과대추정(700~1,200 → 73 MiB), 그 차액의 실체는 Tempo(544 MiB)였다.
- **5.8의 Tempo 조치는 아직 배포되지 않았다** — 설정 파일만 반영됐고 `process_resident_memory_bytes`
  실측 검증이 남아 있다. 예상 효과 300 MiB는 계산값이다.
- **조치를 다 해도 총수요는 RAM을 넘는다** (5.8 추정 ~2,200 MiB vs RAM 1,906). 관측 스택을 앱 호스트에
  동거시키는 구조 자체가 남은 문제다.
- **5.9의 스왑↔GC 상관은 1분 해상도가 한계다** — `rate[2m]` 평활 탓에 swap-in 피크(08:04~08:05)와
  정지 피크(08:03의 2.519s)가 정확히 같은 칸에 안 떨어진다. **같은 2~3분 밴드 안**이라는 것까지가
  이 데이터로 말할 수 있는 전부이고, 인과는 메커니즘과 유휴 대조(13ms vs 2,519ms)로 보강했다.
  개별 정지에 폴트인을 직접 붙이려면 `-Xlog:gc*,safepoint`의 GC 로그 타임스탬프가 필요하다.
- **Tempo 65 MiB는 평시 21시간 값이다** — 비교 대상인 5.7의 544 MiB는 **S 부하테스트 피크** 시점
  `docker stats`라 조건이 같지 않다. 부하 중 블록 완성 버스트에서의 거동은 **다음 사다리 실행에서만**
  확정된다. `mem_limit` 400m이 하드 상한이라 최악의 경우도 컨테이너 단독 OOM-kill로 국한된다.
- ~~**Tempo 내부 지표는 여전히 못 뜬다**~~ → **해소됨.** `docker restart prometheus`로 조치 j가 붙었고
  7.2에서 부하 중 실측까지 마쳤다.
- **런 T의 24.6 RPS는 세그먼트 상태에 종속된 값이다 (7.4)** — 원인은 `global_mutable_segment_rows = 1`로
  확정됐지만, 이 설정은 세그먼트를 계속 늘리는 실험값이다(현재 23개). 세그먼트가 누적되면 스캔 비용이
  다시 오를 수 있으므로 **세그먼트 수를 함께 기록하지 않은 천장 값은 비교 불가**다.
- **적용 시각이 미확정이라 R·S와의 인과를 시각으로 못 묶는다 (7.4)** — `postgresql.auto.conf` mtime이
  남은 확인이다. 또한 R·S 시점의 mutable 세그먼트 크기 기록이 없어 6.4배의 산술 검증은 불가능하다.
- **T는 1회 실행이다** — R/S와 달리 교차 확인이 없다.

---

## 7. 런 T (`20260819-064417`, VU 5/10/15) — 조치 검증 사다리

**목적**: 5.8~5.10의 Tempo 억제가 실제로 먹었는지, 그리고 「천장 14.3」이 메모리 압박 해소 후에도
유효한지 확인한다. **`-Xmx`는 1024 그대로다** — 5.9가 정한 순서대로 이 실행이 Xmx 실험의 대조군이 된다.

- 코드는 R·S와 **같은 빌드**(`838ae893`, 08-17 10:25가 마지막 백엔드 커밋)
- 조치 k(`traceparent` 억제)는 ECR 이미지 미반영 → **전 요청이 트레이싱된다.** Tempo에게 가장 불리한 조건
- VU20 제외 (5.4 권고)

### 7.1 헤드라인 — 「천장 14.3」은 폐기다. 그리고 원인은 우리 조치가 아니다

동일 스크립트·동일 단위로 세 실행을 다시 뽑았다(과거분 재수집 포함):

| VU | | R (08-17) | S (08-18) | **T (08-19)** |
|---|---|---|---|---|
| **5** | RPS / p95 | 12.85 / 499 | 13.27 / 479 | **23.08 / 359** |
| | blks/req | 14,107 | 14,053 | **2,193** |
| | iowait (cores) | 0.637 | 0.549 | **0.001** |
| **10** | RPS / p95 | 14.33 / 959 | 15.67 / 825 | **24.59 / 708** |
| | blks/req | 13,531 | 13,601 | **2,120** |
| | iowait | 0.519 | 0.491 | **0.000** |
| **15** | RPS / p95 | 10.19 / 1,536 | 14.66 / 1,243 | **23.94 / 1,030** |
| | blks/req | **19,915** | 14,077 | **2,153** |
| | iowait | 0.540 | 0.384 | **0.000** |
| | 5xx (전 레벨) | 6 | 9 | **0** |

> **⚠️ 9장 정정: 이 실행은 벡터 검색이 꺼진 상태였다.** 아래 수치는 **BM25 전용 경로** 값이고
> 하이브리드 검색과 비교할 수 없다.

**처리량이 14.3 → 24.6 RPS로 71% 올랐고, VU15 꺾임은 완전히 사라졌으며, 5xx가 0이다.**
헤드라인의 「14.3 RPS = 이 빌드의 실제 천장」은 **메모리 압박 + 아래 DB 조건 하에서의 천장**으로
한정해 읽어야 한다. 이 문서의 확정 문구는 폐기한다.

**다만 이 개선을 5.8~5.10 조치의 공으로 돌릴 수 없다.** 지배 변수는 DB 쪽이다:

| 지표 (VU10 창) | S | T | 배수 |
|---|---|---|---|
| `blks_hit` /s | 166,188 | 52,119 | ÷3.2 |
| **`blks_read` /s (물리 읽기)** | **3,498.6** | **8.8** | **÷396** |
| DB iowait | 0.49 cores | 0.000 | → 0 |
| DB usr+sys | 1.404 | **1.958** | ×1.4 |

요청당 논리 버퍼 접근이 13,601 → 2,120으로 **6.4배** 줄었다. 1장이 "검색 한 번이 106 MB를
256 MB 버퍼 풀로 쓸어낸다"고 했던 것이 **요청당 16.5 MB**가 되어 shared_buffers 안에 들어맞고,
그 결과 물리 읽기가 396분의 1로 붕괴하며 iowait가 0이 됐다. **DB는 이제 I/O 대기가 아니라
순수 CPU로 1.96 cores(2 vCPU의 98%)를 쓴다 — 병목이 I/O에서 CPU로 이동했다.**

**앱 호스트 메모리로는 이 크기를 설명할 수 없다.** 논리 읽기는 플랜이 정하는 값이지 앱이 느리고
빠르고에 따라 변하지 않는다. DB 호스트 페이지캐시도 24시간 내내 550~580 MiB로 평평해 캐시 온도
변화도 아니다. **원인은 DB 쪽이지만 아직 확정되지 않았다** — 설정값 가설은 mtime으로 기각됐고,
현재 유력한 것은 BM25 **세그먼트 누적/병합 주기**다(7.4).

### 7.1.1 24.6도 정점이 아니다 — **VU5에서 이미 포화**다

세 레벨의 RPS가 평평한데 지연만 늘어난다:

| VU | RPS | p50 | p95 | DB usr+sys |
|---|---|---|---|---|
| 5 | **23.08** | 197 | 359 | 1.885 |
| 10 | **24.59** | 378 | 708 | 1.950 |
| 15 | **23.94** | 599 | 1,030 | 1.958 |

**처리량은 ±3% 안에서 평평하고 p95만 359 → 1,030으로 2.9배가 된다.** 교과서적인 포화 곡선이고,
큐가 길어질 뿐 일은 더 안 나온다는 뜻이다. DB `usr+sys`도 이미 **1.885/2 cores(94%)**로
**VU5에서 천장에 닿아 있다.**

즉 **24.6 RPS는 "정점"이 아니라 "포화 고원"의 값**이고, 진짜 무릎(latency가 꺾이기 직전의
최대 처리량)은 **사다리 아래쪽 바깥에 있다.** R·S 시절 사다리를 5부터 시작하도록 올린 것은
당시 정점(VU10)이 담기지 않아서였는데, DB 비용이 6.4배 싸지면서 **정점이 VU5 아래로 내려갔다.**

**→ 사다리를 1/2/3/5로 다시 내렸고, 8장에서 무릎(VU2)과 포화(VU3)를 모두 담았다** (런 U, `20260819-113259`).

### 7.2 Tempo — 조건부 통과. **측정 창을 잘못 잡아 처음엔 과대평가했다**

부하 창(06:45~06:56)만 보고 "피크 88.7 MiB, 목표 크게 통과"로 적었다가, 사후에 5시간
`max_over_time`으로 다시 뜨면서 **틀렸다는 것을 발견했다.** Tempo의 비용은 부하 중이 아니라
**부하가 끝난 뒤 다음 블록 완성 시점에 나타난다.**

#### 실측 — 30분 주기 스파이크, 피크 385.5 MiB

`max_over_time(process_resident_memory_bytes{job="tempo"}[5m])` (08-19, 5분 해상도):

| 시각 (UTC) | RSS |
|---|---|
| 06:45~06:56 (**부하 구간**) | 84~104 MiB |
| **07:05** | **385.5 MiB** ← 부하 직후 첫 블록 완성 |
| 07:35 / 08:05 / 08:35 | 254.8 / 260.5 / 209.8 |
| 09:05 / 09:35 / 10:05 | 208.3 / 248.7 / 242.7 |
| 10:35 / 11:05 | 261.9 / **297.5** |
| 그 외 상시 | 64~97 MiB |

**정확히 :05와 :35, 30분 주기다.** 원인은 `/status/config`에 있다 —
**`ingester.max_block_duration: 30m0s`**. 30분마다 블록을 끊어 완성하고, 그 경로가 5.8의 pprof가
지목한 `CompleteBlock → vparquet4 → parquet-go.newRowGroupRows`다. 5.8은
`complete_block_timeout`을 15m→5m으로 줄였지만 **`max_block_duration` 30m은 손대지 않았다** —
버스트의 *크기*를 정하는 건 이쪽이다.

#### 판정 정정

| 지표 | 5.8 목표 | 처음 보고 | **실제** | 판정 |
|---|---|---|---|---|
| RSS 피크 | < 300 MiB | ~~88.7~~ | **385.5 MiB** | ❌ **초과** |
| `GOMEMLIMIT` 300 | 소프트 상한 | — | 385 > 300 | ⚠️ 버스트 시 넘긴다(소프트라 정상 동작이지만 억제력이 없다) |
| `mem_limit` 400m | 하드 상한 | 여유 | **385.5 / 400 = 96.4%** | 🔴 **OOM-kill까지 3.6%** |
| GC STW 비중 | < 5% | 0.0096% | 0.0096% | ✅ (CPU 여유는 여전히 크다) |

**개선 폭도 다시 써야 한다.** 5.7의 544 MiB는 S 부하 중 `docker stats` 값이고, 여기 385.5는
T 부하 직후 피크다 — 비교 가능한 쌍으로 보면 **544 → 385 (−29%)**이지 −84%가 아니다.
상시 구간이 64~97 MiB로 낮아진 것은 사실이지만, **호스트를 위협하는 건 상시가 아니라 피크다.**

#### 남은 레버

1. **`ingester.max_block_duration` 30m → 10m** — 블록이 3분의 1이면 완성 버스트도 그만큼 작아진다.
   스파이크 자체를 줄이는 유일한 직접 레버다
2. `GOMEMLIMIT` 300 → 200은 **취소하지 않는다.** 7.2 초판에서 "next_gc 53 MiB라 실익 없음"으로
   적었으나 그건 상시 구간만 본 것이다. 버스트 때 넘기고 있으므로 낮추면 완성 중 GC를 더 돌려
   RSS를 누른다 — GC CPU가 0.0096%뿐이라 대가를 치를 여유가 있다
3. 조치 k(`traceparent` 억제)가 ECR 이미지에 들어가면 **부하테스트발 스팬이 1/50로 줄어**
   07:05 같은 부하 직후 피크가 사라진다. 다만 평시 :05/:35 스파이크(200~260)는 남는다
4. `mem_limit` 400m 유지 여부 — 지금은 96.4%까지 찼다. 1~3을 하기 전에 올리면 호스트 스래싱으로
   되돌아가므로, **레버를 먼저 당기고 그래도 부족하면 올린다**

> **방법론 교훈: 배치성 소비자는 부하 창 안에서 측정하면 안 된다.** Tempo·Loki·컴팩션처럼
> 주기적으로 몰아 처리하는 컴포넌트는 **부하 종료 후 최소 한 주기**를 포함해 측정해야 한다.
> 앞으로 사다리 수집 창은 부하 종료 +40분까지 잡는다.

### 7.3 앱 호스트 — 개선됐지만 스왑은 그대로다

| | R 피크 | S 피크 | **T 피크** |
|---|---|---|---|
| MemAvailable 최저 | 92 MiB | 164 MiB | **275 MiB** |
| swap used | 1,530 MiB | — | **1,041 MiB** |
| swap-in | 2,500 pages/s | 1,136 | **1,199** |
| **GC 최대 정지** | **4.15s** | **2.52s** | **0.93s** |
| 커밋 힙 최대 | 984 MiB | 840 | **815** |

- **GC 최대 정지 4.15 → 0.93초.** 5.9의 인과(커밋 힙 → 스왑 → STW 폴트인)와 방향이 일치한다
- **그러나 swap-in 피크는 1,199 pages/s로 S와 사실상 같다.** 스왑 압박 자체는 안 없어졌고,
  커밋 힙도 815 MiB까지 확장했다 — **5.9의 `-Xmx` 조치는 여전히 유효하며, 이 실행이 그 대조군이다**
- 총수요는 RAM 1,906을 여전히 넘는다(5.8·6장 결론 유지)

### 7.4 유효성 검증과 원인 추적 — (9장에서 해결: 벡터 OFF가 원인)

**캐시 오염은 없다.** 레벨당 완료가 4,154건으로 2-term 키워드 풀(2,525개)을 1.6배 넘겨 3장이
경고한 소진 구간에 들어갔으므로 판별법을 돌렸다:

| 레벨 | 완료 2xx | Loki `총:` (코어 실행) | 비율 |
|---|---|---|---|
| 5 | 4,154 | 4,014 | 0.97 |
| 10 | 4,426 | 4,175 | 0.94 |
| 15 | 4,310 | 4,073 | 0.95 |

코어가 매 요청 실제로 돌았다. 캐시 히트가 지배했다면 `총:`이 2,525 근처로 떨어졌어야 한다.
**5~6%의 부족분은 창 경계와 소수의 캐시 히트로 설명되는 수준이고, 6.4배 개선의 설명이 될 수 없다.**

**원인은 특정하지 못했다.** 배제된 것부터:

- ❌ **코드 변경** — 백엔드 마지막 커밋이 `838ae893`(08-17 10:25)로 R·S와 동일 빌드
- ❌ **캐시 오염** — 위 표
- ❌ **DB 호스트 캐시 온도** — 페이지캐시 24시간 평평(550~580 MiB)
- ❌ **앱 호스트 메모리** — 논리 읽기는 앱 속도와 무관

**원인은 BM25 mutable 세그먼트로 좁혀졌다.** `DB_MAX_PARALLEL_WORKERS` 가설은 **기각**됐다 —
`application-prod.properties:38`이 이를 connection-init-sql
(`SET max_parallel_workers_per_gather=${DB_MAX_PARALLEL_WORKERS:0}`)로 적용하는데 compose 기본값이
0이고, deploy 워크플로는 수동 `workflow_dispatch` 입력이 있을 때만 덮는다. 무엇보다
[`2026-08-08-search-checkout-workers-2x2-cachemiss.md`](2026-08-08-search-checkout-workers-2x2-cachemiss.md)에서
**08-08에 이미 0으로 확정**됐다 — R·S 때도 0이었다.

`paradedb.index_info` 실측(08-19)과 `docs/operations/BM25_MUTABLE_SEGMENT_RUNBOOK.md`(08-15)의
전후 기록을 대조하면 인덱스가 **런북의 "튜닝 후" 형태로 바뀌어 있다**:

| | 런북 기준선 (08-15) | 런북 목표 상태 | **현재 실측 (08-19)** |
|---|---|---|---|
| mutable 세그먼트 | **645 docs** | 비움 | **2 docs** |
| 큰 immutable | 14,940 (13 MB) + 1,000×3 | `Segment Count: 4` | **4,565 / 4,585 / 4,870 / 4,565** (각 ~4 MB) |
| BM25 쿼리 `Buffers` | **shared hit=933** | **shared hit=185** | (미측정) |

런북의 조치는 **`ALTER SYSTEM SET paradedb.global_mutable_segment_rows = 50; SELECT pg_reload_conf();`** —
**재배포도 재시작도 필요 없는 런타임 변경**이라 우리 배포 이력에 흔적이 안 남는다. 이것이
R·S(13,500 blks/req)와 T(2,120)를 가르는 유일한 후보다.

**GUC 실측 (08-19).**

```
name                                  | setting | source             | sourcefile                | line
paradedb.global_mutable_segment_rows  | 1       | configuration file | postgresql.auto.conf      | 3
```

```
$ docker exec postgres ls -l /var/lib/postgresql/data/postgresql.auto.conf
-rw------- 1 postgres postgres 176 Aug 15 13:47 ...

$ docker exec postgres cat /var/lib/postgresql/data/postgresql.auto.conf
paradedb.global_mutable_segment_rows  = '1'
paradedb.global_mutuable_segment_rows = '50'      ← 오타 이름. placeholder라 무효
```

#### ⚠️ 정정 — "설정 변경이 원인"은 **기각**이다

이 문서의 앞선 판(08-19 오전)은 `global_mutable_segment_rows = 1`을 R·S와 T를 가른 변수로
**확정이라고 적었다. 틀렸다.** `postgresql.auto.conf`의 mtime이 **08-15 13:47**로
**R(08-17)·S(08-18)보다 앞선다.** 즉 R·S도 이미 `= 1` 상태에서 돌았고, 그런데도
13,500 blks/req를 냈다. **설정값 자체는 차이를 만들지 않았다.**

> 이 오판은 "현재 상태가 런북의 목표 형태와 같다"에서 "그러니 최근에 적용됐다"로 건너뛴 것이다.
> **상태를 보고 변경 시각을 추론하면 안 된다** — 시각은 시각으로 확인해야 한다(그래서 mtime을 떴고,
> 그 결과가 가설을 뒤집었다).

또 하나: 오타 이름(`mutuable`)에도 `ALTER SYSTEM`이 실행된 흔적이 파일에 남아 있다. Postgres는
확장 네임스페이스의 미정의 이름을 **placeholder로 받아 저장까지 한다** — `SHOW`로 조회하면 50이
나오지만 아무 일도 하지 않는다(`pg_settings` 18행에 없다). 세 번째 철자(`mutual`)는
`unrecognized configuration parameter` 에러다 — **placeholder는 한 번이라도 SET된 이름에만 생긴다.**
**GUC 적용 판정은 `SHOW`가 아니라 `pg_settings`의 `source`/`sourcefile`로 해야 한다.**

#### 새 가설 — 값이 아니라 **세그먼트 누적/병합 주기**다

`= 1`은 행마다 세그먼트를 끊으므로 **세그먼트가 계속 늘어난다.** 그리고
`paradedb.global_enable_background_merging = on`(기본값)이 이를 주기적으로 병합한다.
즉 이 설정 하에서 세그먼트 수는 **톱니처럼 오르내린다.**

| 시점 | 세그먼트 상태 | blks/req |
|---|---|---|
| 08-15 13:47 | `= 1` 적용. 런북 §0 기준선은 mutable 645 + 큰 것 14,940 + 1,000×3 | — |
| R (08-17) / S (08-18) | **기록 없음** — 매일 크롤링이 쌓여 tiny 세그먼트가 다수였을 것으로 추정 | **13,531 / 13,601** |
| T (08-19) | **23개** (큰 것 4×~4,600 + tiny 19). 균등한 4개는 **백그라운드 병합의 결과** | **2,120** |

큰 세그먼트가 `14,940 + 1,000×3` → `4,565 / 4,585 / 4,870 / 4,565`로 **균등하게 재편된 것**이
병합이 실제로 돌았다는 증거다. R·S 시점에 tiny 세그먼트가 다수 쌓여 있었다면 스캔이 그만큼
비쌌고, 병합 후인 T에서 싸진 것이 된다.

**이 가설은 확인도 반증도 되지 않았다** — R·S 시점의 `index_info` 기록이 없기 때문이다.
그리고 **가설이 맞다면 성능은 앞으로도 톱니처럼 오르내린다** — T의 24.6 RPS는 "병합 직후"의
값이고, tiny 세그먼트가 다시 쌓이면 내려간다. 런북 자신의 「다음」이
*"세그먼트 수 모니터링 상시화 — 수십 개로 늘면 `mutable_segment_rows`를 올릴 것"*이라고
적어둔 것이 정확히 이 위험이다.

> **⚠️ 2026-08-19 추가 — 비용 모델을 정정한다.** `docs/operations/BM25_MUTABLE_SEGMENT_RUNBOOK.md`의
> 프로덕션 실측(08-15)에 따르면 지배 변수는 세그먼트 **개수**가 아니라 **mutable 세그먼트의 행 수**다.
> mutable 세그먼트는 term dictionary가 없어 **매 쿼리마다 전 문서를 선형 스캔**한다 —
> immutable 17,940 docs가 1.5ms인데 mutable 645 docs가 **148.2ms**(문서당 2,900배)로,
> 전체 3.5% 문서가 쿼리 시간의 99%를 먹는다. 런북은 이 주기적 재발을 **톱니파**로 명명했다.
>
> 이 모델이면 R·S → T·U도 설명된다: `global_mutable_segment_rows = 1`에서 대량 rebuild
> (`ArticleTermService:399`)가 지나가면 **행마다 세그먼트가 생겨 수천 개**가 되고(런북이 정확히
> 이 위험을 경고했다), 이후 백그라운드 병합이 **균등한 4개로 재편**한다 — 08-19에 관측한
> `4,565 / 4,585 / 4,870 / 4,565`가 그 모양이다. **여전히 확정은 아니다**(그 시점 기록 부재).

**닫는 방법**: `paradedb.index_info`를 **주기적으로 기록**하고 사다리 실행과 겹쳐 본다.
세그먼트 수와 blks/req가 함께 움직이면 확정된다. 단발 실행으로는 영원히 못 가른다.
→ **2026-08-19 구현됨** — `Bm25SegmentMetricsScheduler`(5분 간격)가 게이지를 올리고
`collect-results.py`가 레벨별 `segs`/`mut` 열로 찍는다. 배포 후부터 시계열이 쌓인다.

### 7.5 판정

| 주장 | 판정 |
|---|---|
| Tempo 억제가 부하 중에도 유효한가 | ⚠️ **조건부.** 상시는 64~97 MiB로 내려갔지만 30분 주기 완성 버스트 피크가 **385.5 MiB**로 `mem_limit` 400m의 96.4%다. 544 → 385 = **−29%** (7.2) |
| 남은 Tempo 레버는 | **`ingester.max_block_duration` 30m → 10m** (버스트 크기를 정하는 미조정 노브). `GOMEMLIMIT` 200 인하도 유효 — 버스트 때 300을 넘기고 있고 GC CPU는 0.0096%뿐이다 (7.2) |
| 천장 14.3 RPS | ❌ **폐기.** 24.6 RPS, VU15 꺾임 소멸, 5xx 0 |
| 그럼 24.6이 새 천장인가 | ❌ **아니다.** VU5/10/15가 23.1/24.6/23.9로 평평하고 p95만 2.9배 — **VU5에서 이미 포화**다. 정점은 사다리 아래쪽 바깥이라 1/2/3/5로 재측정한다 (7.1.1) |
| 그 개선이 메모리 조치의 성과인가 | ❌ **아니다.** 지배 변수는 DB 논리 읽기 6.4배 감소이고, 원인은 **미확정**이다 — 설정값 가설 기각, 세그먼트 누적/병합이 새 가설(7.4) |
| `-Xmx` 조치가 여전히 필요한가 | ✅ **그렇다.** swap-in 1,199 pages/s, 커밋 힙 815 MiB, GC 정지 0.93s — 5.9의 조건이 그대로 살아 있다 |

---

## 8. 런 U (`20260819-113259`, VU 1/2/3/5) — 무릎을 잡았다

> **⚠️ 9장 정정: 이 실행도 벡터 검색이 꺼진 상태였다(mock 미기동).** 아래의 「무릎 VU2 ≈ 22 RPS」와
> 「SLA 충족 용량」은 **BM25 전용 경로**의 값이다. 하이브리드 검색의 값은 9.2를 볼 것.

7.1.1에서 T가 VU5에서 이미 포화라 정점을 못 담았으므로 사다리를 내려 다시 돌렸다.
**같은 빌드·같은 날·1시간 차이**라 T와 직접 비교 가능하다.

### 8.1 결과 — 무릎은 VU2, 포화는 VU3

| VU | 완료 2xx | RPS | p50 | p95 | DB usr+sys | blks/req | 획득대기 | pend | act |
|---|---|---|---|---|---|---|---|---|---|
| **1** | 2,222 | 12.34 | **76** | **125** | 0.790 | 2,327 | 0.0000 | 0 | 1 |
| **2** | 3,952 | **21.96** | 88 | **145** | 1.580 | 2,098 | 0.0000 | 0 | 3 |
| 3 | 4,220 | 23.44 | 119 | 212 | 1.889 | 2,166 | 0.0000 | 0 | 3 |
| 5 | 4,256 | 23.64 | 198 | 354 | 1.959 | 2,122 | 0.0011 | 1 | 5 |

전 레벨 **5xx 0건, 4xx 0건.**

**한계효용이 VU2에서 꺾인다:**

| 구간 | RPS 증가 | p95 증가 |
|---|---|---|
| VU1 → VU2 | **+78%** | +16% |
| VU2 → VU3 | +6.7% | **+46%** |
| VU3 → VU5 | +0.9% | **+67%** |

VU2까지는 **거의 선형**(DB `usr+sys`가 0.790 → 1.580으로 정확히 2.00배)이고, 그 뒤로는
지연만 사고 처리량은 안 는다. **VU3에서 DB CPU가 1.889/2 cores(94%)로 천장에 닿는다.**

### 8.2 SLA 판정 — 목표의 2.2배를 만족한다

`README`의 기준은 **"10 RPS, 레이턴시 무부하 대비 120%"**다. VU1(12.3 RPS, 커넥션 1개)을
무부하 기준선으로 잡으면:

| VU | RPS | p95 | 기준선 대비 | SLA(≤120%) |
|---|---|---|---|---|
| 1 | 12.34 | 125 | 1.00 | ✅ |
| **2** | **21.96** | **145** | **1.16** | ✅ **통과** |
| 3 | 23.44 | 212 | 1.70 | ❌ |
| 5 | 23.64 | 354 | 2.83 | ❌ |

**SLA를 만족하는 운용 용량은 VU2 ≈ 22 RPS로, 목표 10 RPS의 2.2배다.**
p50 기준으로도 같은 결론이다(88/76 = 1.16 ✅, 119/76 = 1.57 ❌).

### 8.3 부수 확인 — 증폭이 사라진 것이 확정됐다

- **`blks/req`가 전 레벨 2,098~2,327로 평평하다.** 1장이 관측한 "VU15에서 +47%"(19,915)와 같은
  동시성 증폭이 **전혀 없다.** 요청당 16.5 MB가 shared_buffers 256 MB 안에 들어맞으니
  서로를 밀어낼 일 자체가 없다 — 1장 진단이 조건부였다는 7.1의 판단이 확인됐다
- **커넥션은 제약이 아니다.** 획득대기가 VU3까지 0.0000s이고 VU5에서야 0.0011s / pending 1이다.
  R·S에서 VU10에 이미 0.05s / pending 6~9였던 것과 대비된다. **병목은 순수 DB CPU다**
- **iowait 0.008 → 0.000.** VU1에서만 미미하게 잡히고 그 위로는 0이다

### 8.4 유효성 검증

레벨당 완료가 3,952~4,256으로 2-term 키워드 풀(2,525)을 넘겨 3장의 판별법을 돌렸다:

| 레벨 | 완료 2xx | Loki `총:` | 비율 |
|---|---|---|---|
| 2 | 3,952 | 3,787 | 0.96 |
| 5 | 4,256 | 4,043 | 0.95 |

런 T와 같은 0.95~0.96으로, 코어가 매 요청 실제로 돌았다. 캐시 오염 없음.

### 8.5 판정

| 주장 | 판정 |
|---|---|
| 사다리 1/2/3/5가 정점을 담는가 | ✅ **성공.** 무릎(VU2)과 포화(VU3)가 모두 범위 안이다 |
| 새 운용 용량 | **VU2 ≈ 22 RPS** (SLA 120% 이내). 포화 상한은 23.6 RPS |
| 목표(10 RPS) 대비 | **2.2배 여유** |
| 병목 | **DB CPU 단독.** 2 vCPU가 VU3에서 94%, VU5에서 98%. 커넥션·I/O·앱 모두 아니다 |
| 1장의 동시성 증폭 | ❌ **재현되지 않는다.** blks/req가 전 레벨 평평 |

> **다만 8장의 수치도 7.4의 미해결에 종속된다.** 요청당 2,100 blocks라는 전제가 BM25 세그먼트
> 상태에서 오고, 그 상태가 톱니처럼 변한다면 이 22 RPS도 함께 움직인다. **세그먼트 수와 짝지어
> 기록하지 않은 용량 값은 재현되지 않는다.**

---

## 9. 런 X·Y (2026-08-19~20) — **6.4배의 정체는 벡터 검색이 꺼져 있던 것이었다**

### 9.1 결론 먼저

**7장·8장의 모든 사다리(런 T·U·V·W·X)는 벡터 검색이 실행되지 않은 상태에서 측정됐다.**
`/api/search/articles/loadtest`는 쿼리 임베딩을 mock 엔드포인트로 보내는데, mock ECS 서비스는
평시 `desiredCount=0`이고 **`run-prod-test.sh`가 테스트마다 기동/종료한다.** 그런데 이 실행들은
`run-task.sh`를 직접 썼다 — *"ramp-limit-finder는 검색 전용이라 LLM mock이 필요 없다"*는 판단이었고,
**틀렸다.** 검색도 임베딩이 필요하고 그게 mock으로 간다.

`load-test/README.md`에 **"반드시 `run-prod-test.sh`를 쓴다"**고 명시돼 있었다. 읽고도 반대로 했다.

**문서가 예고한 503도 안 났다.** `clova.loadtest-endpoint` 프로퍼티는 설정돼 있고 *서비스만* 죽은
상태라 컨트롤러 가드(`ArticleSearchLoadTestController:66`)가 안 걸렸고, 임베딩이 조용히 실패해
벡터가 0건을 반환했다. 5xx 0으로 깨끗해 보였다 — **가장 위험한 형태의 실패**다.

### 9.2 런 Y가 R·S를 재현한다

`run-prod-test.sh`로 mock을 띄우고 같은 사다리를 돌렸다 (`20260819-235558`, 세그먼트 4개).

| | R (08-17) | S (08-18) | **Y (08-20, 벡터 ON)** |
|---|---|---|---|
| `blks/req` | 13,531~14,107 | 13,601~14,077 | **13,246~13,930** |
| DB iowait (cores) | 0.38~0.64 | 0.38~0.55 | **0.30~0.80** |
| RPS (VU5) | 12.85 | 13.27 | **12.25** |

**세그먼트를 4개로 정리했는데도 R·S와 같은 값이 나온다.** 따라서 R·S → T·U의
`blks/req` 13,566 → 2,120(6.4배)은 **세그먼트 병합이 아니라 벡터가 빠진 것**이다.
7.4가 세운 세그먼트 가설은 이 구간에 대해 **기각**한다.

### 9.3 진짜 하이브리드 검색의 구성

런 Y의 단계별 로그(`ArticleSearchService:730`):

```
BM25:   20~45ms  (100개)                    ←  7~14%
Vector: 244~281ms (30개)                    ←  85%
   ├ embedding: miss(137~187ms, lookup 2ms) ←  mock API 호출
   └ query:     93~105ms                    ←  HNSW 벡터 쿼리
Rerank(NSF): 0ms (130개)
총: 288~323ms
```

**벡터가 요청의 85%이고 BM25는 10% 남짓이다.**

> ⚠️ **임베딩 137~187ms는 mock이 흉내 낸 값이고 README가 "미보정"이라고 명시한 값이다**
> (`MOCK_EMBED_MEDIAN_MS=150`). 실제 Clova 레이턴시를 잰 게 아니다. **요청 시간의 절반가량이
> 캘리브레이션 안 된 상수**이므로, 하이브리드 검색의 절대 처리량을 인용할 때 반드시 붙여야 한다.

### 9.4 그래도 REINDEX는 유효하다 — 다만 4.3배가 아니라 약 2배다

런 W → X(둘 다 벡터 OFF)에서 세그먼트 24 → 4의 효과는 **실측으로 확정**됐다:

| VU5 | W (24 seg, mut 15) | X (4 seg, mut 0) |
|---|---|---|
| RPS | 20.87 | **90.57** (4.34×) |
| p95 | 402 ms | **89 ms** |
| `blks/req` | 2,192 | **1,365** |
| `core-s/req` | 0.094 | **0.018** |
| 단계 로그 BM25 | **303 ms** | **31 ms** |

BM25 303 → 31ms는 직접 증거다. 그러나 하이브리드에서는 BM25가 전체의 10%뿐이라:

```
24 세그먼트:  BM25 ~300 + Vector ~260  ≈ 560ms
 4 세그먼트:  BM25   ~30 + Vector ~260  ≈ 290ms   →  약 2배
```

**(24 세그먼트 × 하이브리드 조합은 실측한 적이 없어 추정이다.)**

### 9.5 세그먼트 단가는 그대로 유효하다

`EXPLAIN (ANALYZE, BUFFERS)` 세 점으로 검증한 모델은 부하 조건과 무관하다:

| 시점 | Segments | mutable docs | Buffers | Execution |
|---|---:|---:|---:|---:|
| 08-15 튜닝 전 | 5 | 645 | 933 | 149.7 ms |
| 08-15 REINDEX 직후 | 4 | 0 | 185 | 1.5 ms |
| 08-19 (정리 전) | 24 | 15 | **367** | 13.97 ms |
| 08-20 (REINDEX 후) | 4 | 0 | **196** | 2.39 ms |

`buffers ≈ 152 + 8.25×segments + 1.16×mutable_docs` — 앞의 두 점으로 맞춘 식이 네 번째 점을
**6% 오차로 예측**했다(185 예측 / 196 실측). **세그먼트당 8.25블록 · 0.45ms는 실재한다.**

### 9.6 정정 목록

| 대상 | 정정 |
|---|---|
| 헤드라인 「천장 14.3 RPS」 폐기 | **폐기를 철회한다.** R·S가 옳았고 런 Y가 재현했다 |
| 7.1 「24.6 RPS」, 8장 「22 RPS」 | **BM25 전용 경로 값.** 하이브리드 검색 값이 아니다 |
| 7.4 「6.4배 원인 미상」 | **해결 — 벡터 OFF.** 세그먼트 가설은 이 구간에 한해 기각 |
| 7.1.1 「VU5에서 포화」 | BM25 전용 조건 한정. 하이브리드는 VU5에서 12 RPS·DB 미포화 |
| 축 B (세그먼트 → 블록) | W→X에서 유효하나 **BM25 전용 조건**. 하이브리드 기여는 약 10% |
| 8.2 「SLA 충족 22 RPS」 | 무효 — 벡터 없는 경로였다 |

### 9.7 방법론 교훈

1. **`run-task.sh`로 검색 시나리오를 돌리면 벡터가 조용히 빠진다.** README가 예고한 503은
   안 나온다(엔드포인트 프로퍼티는 설정돼 있고 서비스만 죽어서). **5xx가 0이어도 정상이 아닐 수 있다.**
2. **단계별 로그를 먼저 봤어야 했다.** `[검색] keyword=... | BM25: {}ms, Vector: {}ms ... | 총: {}ms`
   한 줄이 이 전부를 즉시 드러냈는데, 집계 지표(`blks/req`, `core-s/req`)만 며칠 쫓았다.
   **`(0개)`와 `embedding: miss(0ms)`는 처음부터 로그에 찍혀 있었다.**
3. **실행마다 "무엇이 실제로 실행됐는지"를 검증 항목에 넣어야 한다.** 캐시 오염(Loki `총:`)은
   매번 확인했으면서 "벡터가 돌았는가"는 확인 항목에 없었다.

---

## 미기록 / 다음

- [x] 사다리 상향 (선행 문서 7장 1순위) — 이 문서. 정점 VU10 = 14.3 RPS 확정
- [x] **앱 호스트 메모리 분해** — 5장. 1순위는 백엔드 JVM의 **커밋 힙 984MB**(used는 337MB —
      종전 "힙 276MB/1,024MB로 여유"는 used 기준 오독이었다. 단 이 337도 한 시점 샘플이라
      **5.9에서 601/652로 정정**), 2순위는 k6 URL 카디널리티로 부푼
      Prometheus(실행당 시계열 15만~19만). 관측 스택이 백엔드와 동거하며 RAM 1,906MB를 나눠 쓴 결과
      총수요가 RAM의 1.6배(3.0GB)까지 갔다
- [x] **k6 라벨 카디널리티 제거(1) + TREND_STATS 축소(1′)** — 5.7. 산출식 교체·캘리브레이션까지 완료.
      1′을 먼저 하고 1을 나중에 하려던 순서는 **동시 시행으로 바꿨다** — 둘 다 실행 레이어 한 줄이라
      변경을 쪼갤 이유가 없고, 쪼개면 "새 실행 + 옛 산출식"이라는 조용히 틀리는 조합이 생긴다
- [x] **전환 실행(S, `20260818-075257`, VU 5/10/15)** — 시계열 86개, Prometheus 73 MiB,
      관측 스택 생존. `docker stats` 실측으로 5.4 장부 확정 (5.7)
- [x] **Tempo 억제 — 원인 분해와 설정 반영 완료 (5.8).** 544 MiB는 필요량이 아니라 GC 목표였다
      (live heap은 85 MB, GC CPU 0.06%). `GOMEMLIMIT=300MiB` + `mem_limit: 400m`이 핵심이고,
      `tempo.yml`의 기본값 6종이 이 호스트 규모와 3~100배 어긋나 있던 것을 맞췄다.
      **`TRACING_SAMPLING=0.1`은 틀린 레버로 판명나 폐기** — 평시 트래픽이 0.15 RPS뿐이라 관측성만
      죽는다. 대신 k6가 `traceparent`(sampled=0)를 보내 부하테스트 트레이스만 1/50로 줄인다.
      `block_retention` 168h는 메모리와 무관해(블록 170개) 유지
- [x] **Tempo 조치 a~i 적용 확인 + 메모리 실측 (5.10)** — `GOMEMLIMIT`/`mem_limit`은 `docker inspect`로,
      `tempo.yml` 8종은 `/status/config`로 확인. **평시 RSS 544 → 65.43 MiB (−88%)**, 가동 21시간
      OOM-kill 0회. 예상치 300 MiB를 크게 밑돈다. 단 **부하 중 거동은 다음 사다리 실행에서 확정**
- [ ] **조치 j 적용 — `docker restart prometheus`** — SIGHUP은 이미 시도했고 "리로드 성공" 로그까지
      찍혔지만 **bind mount inode** 때문에 옛 파일을 다시 읽었을 뿐이다(5.10). 재기동이라야 한다.
      붙은 뒤 `GCCPUFraction` < 5% 확인 → 통과하면 `GOMEMLIMIT`을 200MiB까지 조인다
- [ ] **관측 스택 배포 경로 정하기** — `deploy.sh`가 prometheus/tempo를 안 건드리는 데다,
      bind mount inode 탓에 **"파일은 새것, 컨테이너는 옛 설정, 로그는 성공"**이라는 조용한 실패가
      난다(5.10). `deploy.sh`에 넣든 별도 절차로 문서화하든 정할 것.
      **nginx 전용으로 알려진 CLAUDE.md의 inode 주의사항을 관측 스택 전체로 확장**해야 한다
- [x] **GC 정지의 원인 규명 (5.9)** — 5.7 표의 「최대 정지 4.15s → 2.52s」에 원인을 붙였다. 정상 young GC는
      23~56ms인데 하나가 **2,519ms**이고, 그 시점 Eden은 17 MiB로 오히려 일감이 적었다. 유휴 대비 190배
      (13ms vs 2,519ms)이며 달라진 변수는 swap-in(1.7 → 1,136 pages/s)뿐 — **STW 안의 스왑 폴트인**이다.
      **부수 정정: 5.2/5.6의 「used 최대 337MB」는 한 시점 샘플이고 실측 피크는 601(R)/652(S) MiB다**
- [x] **`-Xmx` 사이징 확정 — 512 (5.11, 2026-08-20).** 5.9의 "live 300~341의 1.5배라 빠듯"을
      4일치 실측으로 갈아치웠다. 하한(live **p99 253.5** × 2배 = **506**)과 상한(호스트 예산 역산
      = **549**)이 **[506, 549]**에서 겹치고 그 안의 관례값은 512뿐이다 — 640은 예산 +105 초과,
      384는 하한 미달. 근거가 바뀐 게 아니라 **강해졌다**(1.5배 → 2.0배). 옵션 세트와 각 옵션의
      이유는 5.11.8
- [x] **`-Xmx512m` 적용 + 크롤링 창 검증 완료 (5.12, 08-20 20:37 KST 배포).**
      **5.9의 인과가 확증됐다** — Evacuation STW가 50.20 → 25.55초(**−49%**)인데 **횟수는 100 → 128회로
      오히려 늘었다.** 1회 비용만 0.502 → 0.199초로 떨어진 것이라, "폴트인할 차가운 페이지가 없어졌다"
      외에 설명이 없다. 커밋 힙 유휴 543 → 276 / 크롤링 900+ → 486, MemAvailable 302 → 495,
      익명 총수요 2,147 → 2,029, **major GC 0회**(512는 부족하지 않다)
- [ ] **🔴 `G1PeriodicGCSystemLoadThreshold=1` 검증 — 다음 크롤링 창(04:00~05:30 KST).**
      5.11.8에서 넣은 `G1PeriodicGCInterval=60000`이 **18.344초(28회)를 새로 만들었고 그날 밤 최악의
      정지 15.877초가 바로 이것**이다(5.12.2). 이 GC는 "60초간 GC가 없었던 = 페이지가 가장 차가운"
      시점에만 발동해 1회 평균이 Evacuation의 3.3배다. 다만 크롤링 창 밖에서는 0.36 s/h로 사실상
      공짜이고 유휴 반납 −267 MiB를 벌어준 것도 이 옵션이라 **끄지 않고 load1 > 1일 때만 취소**한다.
      **기대: STW 합계 46.8 → 약 28초(08-20 대비 −45%), 최대 정지는 Evacuation 계열(11.5초)로 하강**
- [ ] **swap-in 피크가 함께 내려가는지 확인 (5.12.3의 부수 예측)** — 커밋 힙을 400 MiB 줄였는데
      폴트인 총량은 2,111 → **2,335 pages/s로 +11%**였다. 임계 적용 후 내려가면 그 폴트인의 일부가
      **주기 GC 자신이 만든 것**이었다는 뜻이고, 안 내려가면 스왑 압력은 순수하게 Chrome 몫이다
- [x] **최악의 GC 정지는 부하테스트가 아니라 매일 04:07 크롤링 창이었다 (5.11.1).**
      4일간 2초 초과 정지 13건 중 7건이 크롤링 창이고 최대 **21.602초** — 부하테스트 최악(런 R 4.152초)의
      **5.2배**다. 04:07/04:37/05:07이 `CrawlingScheduler`의 04:00·04:30·05:00과 버킷 단위로 붙는다.
      압력원은 k6가 아니라 **같은 컨테이너에서 뜨는 headless Chrome**이다.
      결정적 실측: 그 창의 **live set은 161~180 MiB, used 피크 510**인데 커밋 힙은 900+였다 —
      **정지의 대가를 치른 720 MiB는 전부 "안 쓰는 커밋"이다**
- [ ] **backend `mem_limit`(5.6 조치 3)은 보류 — 근거 기록 (5.11.9).** 크롤링 창에 Chrome이 같은
      컨테이너에 뜨므로 JVM ~974 + Chrome 200~400이 되어 안전장치가 아니라 **매일 밤 OOM-kill 예약**이
      된다. 04:00~05:30 KST에 컨테이너 RSS를 먼저 잴 것. `mem_swappiness: 0`도 같은 이유로 그 뒤
- [ ] **예산 1,010 MiB의 추정 칸 확정 (5.11.9)** — (a) `loki`·`grafana`·`promtail`은 스크레이프가
      없어 250이 추정이다(tempo의 조치 j처럼 붙일 것), (b) JVM 네이티브 160도 추정 —
      `-XX:NativeMemoryTracking=summary` 한 번이면 5.4 장부의 마지막 추정 칸이 사라진다
- [ ] **live set p99 월 단위 감시** — 하한 A(=p99×2)가 512를 정당화하는 근거다. Caffeine `aiSummary`·
      `SearchQueryEmbedding` 캐시는 트래픽·데이터와 함께 자라므로 **p99가 300을 넘으면 512는 무효**가 된다
- [x] **천장 재측정 (런 T, `20260819-064417`) — 7장.** VU10 **24.59 RPS**, VU15 꺾임 소멸, 5xx 0으로
      「천장 14.3」 폐기
- [x] **Tempo 조치 검증 — ⚠️ 조건부 통과 (7.2).** 상시는 64~97 MiB로 내려갔으나 **30분 주기 블록 완성
      버스트 피크가 385.5 MiB**로 `mem_limit` 400m의 96.4%다. 544 → 385 = **−29%**(−84%가 아니다)
- [ ] **🔴 `ingester.max_block_duration` 30m → 10m** — 5.8이 `complete_block_timeout`만 줄이고
      **버스트 크기를 정하는 이 노브는 안 건드렸다**(7.2). 스파이크를 직접 줄이는 유일한 레버
- [ ] **`GOMEMLIMIT` 300 → 200** — "실익 없음"으로 적었던 판단을 뒤집는다. 상시 구간만 보고 내린
      결론이었고 버스트 때는 300을 넘긴다. GC CPU가 0.0096%뿐이라 대가를 치를 여유가 크다 (7.2)
- [x] **`global_mutable_segment_rows = 1` 원인설 기각 (7.4)** — `postgresql.auto.conf` mtime이
      **08-15 13:47**로 R·S보다 앞선다. R·S도 `= 1`에서 13,500 blks/req를 냈으므로 설정값은 차이를
      만들지 않았다. `DB_MAX_PARALLEL_WORKERS`도 기각(08-08에 0 확정)
- [x] **BM25 mutable 세그먼트가 읽기 비용을 지배한다는 것을 부하 환경에서 확증 (2026-08-19).**
      `mut`(mutable 문서 수) 지표를 배포하고 사다리를 2회 독립 실행해, mutable 15행이
      **요청당 DB CPU +12% / 처리량 −10%**를 만드는 것을 `blks/req` 불변인 채로 관측했다.
      런북의 0.23 ms/doc 선형 모델이 재현됐다.
      → [`2026-08-19-mutable-segment-rows-ab.md`](2026-08-19-mutable-segment-rows-ab.md) 3.6
- [x] **6.4배의 원인 확정 — 벡터 검색 미실행 (9장, 2026-08-20).** `run-task.sh`로 돌려 임베딩 mock이
      미기동이었고 벡터가 조용히 0건을 반환했다. 벡터를 켠 런 Y가 세그먼트 4개에서도
      `blks/req` 13,246~13,930으로 R·S를 재현했다. **세그먼트 가설은 이 구간에 한해 기각**
- [ ] **🔴 하이브리드 기준선 재수립** — 7·8장의 사다리 수치는 전부 **BM25 전용**이다.
      `run-prod-test.sh`로 사다리를 다시 세워야 실사용 경로의 천장·무릎을 말할 수 있다.
      현재 유일한 하이브리드 점: 런 Y (VU5 12.25 RPS, p95 539ms, 세그먼트 4)
- [ ] **`MOCK_EMBED_MEDIAN_MS` 캘리브레이션** — 하이브리드 요청의 절반가량(137~187ms)이
      **미보정 상수**다(README 명시). 실 Clova 레이턴시를 분리 측정할 지표가 필요하다 —
      `search_query_embedding_seconds`가 캐시 히트와 실호출을 합산해 못 쓴다
- [ ] **실행별 "무엇이 실제로 돌았는가" 검증 항목화 (9.7)** — 캐시 오염(Loki `총:`)은 매번 봤으면서
      벡터 실행 여부는 확인 항목에 없었다. `collect-results.py`에 벡터 결과 수 열을 넣는 것을 검토
- [ ] **🔴 남은 가설 — 세그먼트 누적/병합 주기 (7.4)** — `= 1`은 세그먼트를 계속 늘리고
      백그라운드 병합이 주기적으로 되감는다. T는 "병합 직후" 값일 수 있다.
      **`paradedb.index_info`를 시간당 기록**해 세그먼트 수와 blks/req가 함께 움직이는지 볼 것 —
      단발 실행으로는 영원히 못 가른다
- [ ] **런북 수정 (`docs/operations/BM25_MUTABLE_SEGMENT_RUNBOOK.md`)** — (a) 운영 실제값이 1인데 §5는
      50으로 되돌리라고 되어 있어 문서와 현실이 어긋난다, (b) **`SHOW`만으로 적용 판정 금지** 경고 추가 —
      오타 이름(`mutuable`)이 placeholder로 50을 반환해 실제로 한 번 속았다. `pg_settings`의
      `source`/`sourcefile`로 볼 것. `postgresql.auto.conf`에 남은 그 오타 줄도 함께 정리
- [ ] **수집 창을 부하 종료 +40분까지 확장** — 배치성 소비자(Tempo 블록 완성 등)는 부하 창 안에서만
      재면 과소평가된다. 7.2에서 실제로 385를 88.7로 잘못 보고했다
- [x] **런 U (`20260819-113259`, VU 1/2/3/5) — 8장.** 무릎 **VU2 ≈ 22 RPS**(SLA 120% 이내),
      포화 VU3(DB CPU 94%), 상한 23.6 RPS. 전 레벨 5xx 0, `blks/req` 평평(증폭 소멸 확정),
      커넥션은 제약이 아님(획득대기 VU3까지 0). **목표 10 RPS의 2.2배**
- [ ] **런 U의 Tempo 피크 확인** — 부하 종료(11:47) +40분 창으로 `max_over_time(...[5m])` 확인.
      7.2의 교훈대로 부하 창만 보면 과소평가된다
- [x] **`index_info` 정기 기록 — 구현 완료(배포 대기).** `Bm25SegmentMetricsScheduler`가 5분 간격으로
      `bm25_index_segments` 등 게이지를 올리고 `collect-results.py`가 레벨별 `segs`/`mut` 열로 찍는다.
      스크립트에서 운영 DB로 붙을 경로가 없어(`.env`는 로컬 DB) **앱 경유가 유일한 구조**였다.
      런 U까지는 지표 이전이라 `-`로 나온다
- [x] **`global_mutable_segment_rows = 50` 설정 완료 (2026-08-19)** — 별도 세션에서
      `setting=50 / source=configuration file` 확인. 중간에 기본값으로 RESET한 것은
      **틀린 조치였다.** 런북이 08-15 프로덕션 실측으로 임계치를 산정해 `50`을 결론냈고(1 = 세그먼트
      폭증으로 금지, 1000 = 최악 지연 230ms), P0의 `= 50` 항목이 체크되지 않은 채 남아 있던 것이
      운영에 `1`이 박혀 있던 원인이다. 별도 세션에서 값 확인까지 할 것 —
      같은 배치의 `SHOW`는 SIGHUP 전 값을 읽는다(런북 「설정 레버는 살아 있다」).
      **쓰기가 쌓인 뒤에만 측정된다**(쓰기 경로 전용 노브) → 야간 크롤링 후 사다리.
      절차·판정 기준은 [`2026-08-19-mutable-segment-rows-ab.md`](2026-08-19-mutable-segment-rows-ab.md)
- [ ] **k6 이미지 재빌드·푸시** — 시나리오 기본값(5/10/15/20)과 **5.8 조치 k의 `traceparent` 억제**가
      ECR 이미지에 반영돼야 한다. 둘 다 `scenarios/`·`lib/`라 실행 레이어로는 못 넘긴다
- [ ] 과거 실행 재수집 시 `VU_LEVELS=1,2,5,10`을 지정해야 한다 (스크립트 기본값이 바뀜)
- [ ] Q/R 반복 실행 — 정점은 교차 확인됐지만 VU15 꺾임 폭은 1회뿐
- [ ] C안: 소프트 삭제 시 청크 정리 (선행 문서 7장에서 이월)
- [ ] `ef_search`/`candidateLimit`(250/200) 하향 스윕 — VU15의 blks/req +47%는 이 레버의 근거를 강화한다
- [ ] recall 측정 (`VectorSearchAccuracyService`) — 기존 부채

---

## 부록 — 확인 명령어 모음

### A. `ALTER SYSTEM` 적용 시각 (7.4의 마지막 고리)

DB 호스트(운영 Postgres 서버)에서. **postgres가 컨테이너로 돌아 호스트 경로엔 파일이 없다** —
`ls -l $PGDATA/...`는 실패한다. 컨테이너 안에서 봐야 한다.

```bash
docker ps --filter name=postgres --format '{{.Names}}'
docker exec postgres ls -l /var/lib/postgresql/data/postgresql.auto.conf
docker exec postgres cat /var/lib/postgresql/data/postgresql.auto.conf
```

**✅ 실행됨 (08-19). 결과: `Aug 15 13:47` — S·T 사이가 아니라 R보다도 앞선다.**
따라서 설정값 가설은 **기각**됐고 7.4를 새 가설(세그먼트 누적/병합)로 다시 썼다.
파일에는 오타 이름(`global_mutuable_segment_rows = '50'`) 줄도 함께 남아 있었다 — placeholder라 무효다.

### B. 컨테이너별 피크 RSS (5.4 장부 확정 / 5.5가 요청한 계측)

앱 호스트. 런 T 구간은 **06:44:45~06:55:00 UTC**이고,
`/tmp/memstat.log`는 `HH:MM:SS <name> <used> / <limit>` 형식이라 첫 필드 문자열 비교로 자른다.

```bash
# 컨테이너별 피크 (MiB 정규화)
awk '$1>="06:44:00" && $1<="06:56:00" {
       v=$3; u=v; gsub(/[0-9.]/,"",u); gsub(/[^0-9.]/,"",v); v+=0;
       if(u=="GiB")v*=1024; else if(u=="KiB")v/=1024;
       if(v>m[$2])m[$2]=v
     } END{ for(c in m) printf "%-28s %8.1f MiB\n", c, m[c] }' /tmp/memstat.log | sort -k2 -nr

# 전체 합계의 피크 시각
awk '$1>="06:44:00" && $1<="06:56:00" {
       v=$3; u=v; gsub(/[0-9.]/,"",u); gsub(/[^0-9.]/,"",v); v+=0;
       if(u=="GiB")v*=1024; else if(u=="KiB")v/=1024; s[$1]+=v
     } END{ for(t in s) if(s[t]>mx){mx=s[t]; at=t}; printf "피크 합계 %.1f MiB @ %s\n", mx, at }' /tmp/memstat.log
```

이걸로 5.4의 추정 칸이 **부하 중 실측**으로 바뀐다 (5.7의 `docker stats`는 S 실행 1회 스냅샷이었다).

> **⚠️ 08-19 시도 결과: 매칭 0건.** 로그 마지막 줄이 `08:06:43`인데 그 줄의 limit 칸이 `1.861GiB`
> (= 호스트 RAM, 즉 `mem_limit` 없음)라 **08-18 기록**이다 — tempo가 `mem_limit: 400m`을 받은 건
> 08-18 09:30이다. 즉 **루프가 런 T 때 돌고 있지 않았다.** 재시도 전에 살아 있는지부터 확인할 것:
>
> ```bash
> pgrep -af "docker stats" ; ls -l /tmp/memstat.log
> awk '{print $1}' /tmp/memstat.log | sort -u | sed -n '1p;$p'   # 로그가 덮는 시각 범위
> ```
>
> **또한 이 측정은 부하 창만으로는 부족하다** — 7.2에서 드러났듯 Tempo 피크는 부하 종료 후
> 다음 블록 완성(최대 30분 뒤)에 온다. 루프를 **부하 종료 +40분까지** 돌릴 것.

수집이 끝나면 로그 루프를 정리한다:

```bash
pkill -f "docker stats"
```

### C. 참고 — 이번에 쓴 검증 쿼리 모음

```promql
# Tempo (7.2)
max_over_time(process_resident_memory_bytes{job="tempo"}[11m])/1024/1024   # 목표 < 300
max_over_time(go_memstats_next_gc_bytes{job="tempo"}[11m])/1024/1024
rate(go_gc_duration_seconds_sum{job="tempo"}[10m]) * 100                   # GC STW %, 목표 < 5
go_gc_gomemlimit_bytes{job="tempo"}/1024/1024                              # 적용 확인 (300)

# 앱 호스트 / JVM (5.9, 7.3)
max_over_time(jvm_gc_pause_seconds_max{cause="G1 Evacuation Pause"}[11m])
max_over_time(sum(jvm_memory_committed_bytes{area="heap"})[11m:15s])/1024/1024
max_over_time(rate(node_vmstat_pswpin{job="node-exporter"}[2m])[11m:30s])
min_over_time(node_memory_MemAvailable_bytes{job="node-exporter"}[11m])/1024/1024

# DB (7.1)
rate(pg_stat_database_blks_hit{datname="small_town"}[3m])
rate(pg_stat_database_blks_read{datname="small_town"}[3m])                 # 물리 읽기
```

```logql
# 캐시 오염 판별 (3장, 7.4) — 완료 2xx와 대조
sum(count_over_time({job="small-town"} |= "총:" [3m]))
```

```sql
-- BM25 세그먼트 상태 (7.4). 사다리 실행 때마다 같이 남길 것
SELECT segno, mutable, num_docs, pg_size_pretty(byte_size::bigint) sz
FROM paradedb.index_info('article_analyzed_content_bm25_idx') ORDER BY mutable DESC, num_docs DESC;

-- GUC 적용 여부는 SHOW가 아니라 pg_settings로 (7.4의 placeholder 함정)
SELECT name, setting, source, sourcefile, sourceline FROM pg_settings WHERE name LIKE 'paradedb%';
```
